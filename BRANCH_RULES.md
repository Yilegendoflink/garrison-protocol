# 职业分支规则记录与运行接入

记录日期：2026-09-13。已检索 [PRTS 分支一览](https://prts.wiki/w/分支一览)（修订326449）和 [分支特性细则](https://prts.wiki/w/分支特性信息/data)（修订427899），与本项目固定历史数据交叉对应。

机器记录：`data/prts/branch-rules.json`，包含71个历史分支，其中56个出现在当前固定预设中。每项保存内部ID、中文名称、代表干员、基础特性、来源修订链接、本期适用性、运行行为及未完成项。原始Wiki全文仅保留在被Git忽略的研究缓存；构建使用已经记录的JSON，不依赖在线Wiki。

## 运行逻辑

`dist/native-branches.js` 定义分支基础行为，`dist/native-battle.js` 负责调用。职业不再直接决定“医疗一定治疗、辅助一定法术攻击”。游戏大厅、战场顶部和干员详情均可打开分支规则。

当前已分别接入：

- 咒愈师：索敌、对空、基础法术伤害；有合法友方目标时按伤害结算值的50%治疗。屏障和禁疗生效；治疗基数不按敌人的剩余HP截短。
- 医师／群愈师／疗养师／链愈师／行医：按各自目标数量、距离衰减、弹跳衰减及元素治疗接口处理，不套用单体医疗。行医的实际元素损伤产生仍依赖敌人／技能实现。
- 吟游者：不进行普通攻击，按攻击力提供持续生命回复；持续回复与普通治疗统计、禁疗约束分开。专属技能替换特性的效果仍需逐技能接入。
- 阵法术师：非技能期不攻击并获得防御／法抗增益，技能期间群体攻击。
- 解放者：非技能期不攻击、不阻挡，按秒积累特性攻击加成，技能结束重置。
- 武者／收割者／不屈者：限制外部治疗，保留自身特性／技能恢复的处理入口；武者和收割者分别处理命中恢复。
- 强攻手／重剑手／推击手、剑豪、散射手、炮手／扩散术师／轰击术师、撼地者／要塞：分别处理按阻挡数、多次攻击、范围内全体或独立溅射。
- 猎手、秘术师、回环射手、驭械术师：分别处理独立武器装填、能量储存、投射物回收门槛及普通浮游单元伤害。装填不复用技能弹药，攻击取消时不提前消耗尚未发射的储能。
- 速射手／神射手／攻城手、领主／教官、凝滞师／链术师、伏击客、怪杰：分别处理目标优先级、攻击倍率、停顿、闪避／嘲讽和非致命生命流失。
- 对空：分支默认值来自 PRTS「分支特性信息/data」的 `antiAir`（本地 `data/prts/branch-rules.json` 与 `BRANCH_POLICIES` 逐条一致，`native-battle.behavior()` 是唯一读取口）。**部分地面干员的技能会改写这一点**，依据是 PRTS 干员页技能备注里的「※可对空／※不可对空」，记在 `native-branches.js` 的 `SKILL_ANTIAIR`：德克萨斯「剑雨」、焰尾「红松林」、忍冬「坠刃拷问」、塞雷娅「钙质化」、玛恩纳「未照耀的荣光」、缄默德克萨斯「剑雨滂沱」、锏「归于宁静」、泥岩「秽壤的血脉」、号角「照明榴弹」可对空；银灰「雪境生存法则」缩小范围后**不再**攻击空中单位。瞬时／被动技没有生效期，覆盖只作用于开启那一帧（它们的伤害就在那一帧结算）。回归：`tests/native-air-targeting.test.mjs`。

## 实现边界

上述为分支基础层，不表示全干员专属技能、天赋、模组和全部动作时序已还原。替身切换、战术点、召唤／陷阱／支援装置、起飞、费用机制，以及特殊弹跳／余震／回旋时序等继续在记录中标注待补齐。不要把“已有规则记录”当作“完全实现或通过验收”。

更新资料使用 `npm run data:branches`；打包使用 `npm run build`。按用户现行约定，本次仅构建并发布到Pages供手动验收，不运行测试。

## 同批交互修正

高台新增立面、浅色顶面和标识，地面使用深色平面纹理，不可部署区使用斜纹，并提供图例。出怪预览与实际战斗共用 `native-waves.js`；木桩单独显示目标数。休整期可直接把场上干员拖回整备区，拖到新地块后重新选择朝向；确认前保持原位，取消不改变阵地，满手牌时拒绝收回。
