# 战斗部署费用（cost）交互审计

本审计以 PRTS 的部署费用条目、卫戍协议帮助页和项目固定数据为准。项目固定名单是 `alliance-lower-post-2026-03-27` 的 112 名干员；完整逐名描述见 [`operator-behavior-audit.json`](<D:/workwork/ark/data/modes/alliance-lower/operator-behavior-audit.json>)。

## 费用的正确语义

`funds` 是卫戍协议整备阶段的调度资金，负责购买、刷新、升级和装备。`cost` 是作战阶段的部署费用，负责部署、撤退返费、费用型技能、费用型天赋和分支特性。琳琅诗怀雅的“金币”不是整备资金，也不是全局通用货币，而是把技能技力上限改写为金币上限的专属技能资源；其他模式中的金币/收藏品奖励仍属于模式账本，不能与战斗费用合并。

PRTS 的“卫戍协议：盟约下半/战场一览”记录为初始 COST 0、上限 99；本项目按产品要求将战斗初始 `cost` 覆盖为 20。这个覆盖只改变本项目的起始值，不应把 `funds` 或金币并入 `cost`。

PRTS 对普通部署费用的模型是：

```text
部署费用属性 = (初始部署费用 + 基础费用增减) × 费用倍率 + 实时费用增减
```

需要单独保存：关卡初始费用、当前费用、自然回复间隔、费用上限、费用下限、基础部署费用、实例本次部署费用、返费倍率、返费上限、可返费标记和再部署惩罚层数。一般返费为本次部署费用的 50% 并受基础部署费用限制；冲锋手通常 100% 返还，行商为 0%。负费用下限开启时，费用型消耗可能透支到负数，特殊能力还存在是否考虑负数部分的差异。

来源：

- PRTS [部署费用](https://prts.wiki/w/%E9%83%A8%E7%BD%B2%E8%B4%B9%E7%94%A8)
- PRTS [分支特性信息/data](https://prts.wiki/w/%E5%88%86%E6%94%AF%E7%89%B9%E6%80%A7%E4%BF%A1%E6%81%AF/data)
- PRTS [卫戍协议/帮助](https://prts.wiki/w/%E5%8D%AB%E6%88%8D%E5%8D%8F%E8%AE%AE/%E5%B8%AE%E5%8A%A9)

## 固定 112 名中的费用交互

| 干员 | 交互 |
| --- | --- |
| 野鬃、风笛 | 冲锋手分支：击杀获得 1 点费用，撤退返还初始部署费用。野鬃另有未部署近卫减费。 |
| 德克萨斯 | S1/S2 立即获得费用；天赋增加初始部署费用；模组隐藏字段 `runtime_cost=-4`，首次部署再减费。 |
| 砾 | 自身部署费用降低；按初始部署费用不超过 10 的单位筛选防御加成；模组把返费倍率提高到 80%。 |
| 忍冬 | S1/S2/S3 直接或持续回费；天赋提高费用自然回复速度。 |
| 伺夜 | S1/S2 直接回费、狼群击杀回费、S3 持续回费；战术点是 0 费实体。 |
| 焰尾 | S1/S2 直接回费，S3 持续回费。 |
| 伊内丝 | S3 首次部署不消耗费用；S1/S2/S3 按攻击或命中回费。 |
| 凛御银灰 | 费用排序、待部署区减费、费用交换、费用最高/最低筛选，以及技能期间加入可部署的“风雪之眼”。 |
| 缪尔赛思 | S1/S2/S3 直接或持续回费；莱茵生命干员部署费用降低。战术点/流形部署属于独立实体规则。 |
| 琳琅诗怀雅 | S1/S2 视为部署时已开启的被动技能并消耗金币，S3 用金币作为技能资源、击杀获得金币并在关闭技能时按金币结算；天赋把行商耗费转金币，并可消耗费用免死。 |

按技能、天赋和分支文本统计是上表 11 名；再把模组和 token 属性纳入后，本期实际有 15 名存在费用交互。新增的 4 名是：格雷伊、莫斯提马、寒檀（模组降低自身部署费用）和凯瑟琳（模组降低支援装置 token 的部署费用）。

固定名单的模组还会改变费用语义：风笛的 CHG-X 返还本次实际部署费用，野鬃同样覆盖返费上限；德克萨斯首次部署 `runtime_cost=-4`；焰尾首次部署 `runtime_cost=-4`；伊内丝存在与首次免费部署逻辑相关的 `runtime_cost=-9999` 隐藏覆盖；砾的返费倍率提高到 80%；琳琅诗怀雅可把行商周期耗费从 3 降为 2。这些字段已经被固定数据保存，但当前公共结算尚未全部读取。

除逐名效果外，固定名单还必须应用分支规则：

- `charger`：野鬃、风笛的击杀回费与返费。
- `merchant`：琳琅诗怀雅持续扣费、自动撤退、零返费。
- `tactician`：伺夜、缪尔赛思的战术点/援军费用为 0，并有独立生命周期。
- `agent`：伊内丝的首次免费部署和离场后重新部署条件。

## 固定名单之外的已知交互

固定数据还检出 43 名不属于本期 112 人名单、但未来扩展时必须纳入的干员：

`CONFESS-47、芬、香草、翎羽、讯使、清道夫、预备干员-先锋、红豆、桃金娘、豆苗、冬时、断罪者、孑、苏苏洛、凛冬、贾维、青枳、郁金香(char_513_apionr)、红隼、松桐、苇草、历阵锐枪芬、极境、万顷、夜半、渡桥、晓歌、谜图、齐尔查克、寻澜、哈蒂娅、四月、截云、乌有、裁度、灰烬、推进之王、嵯峨、琴柳、郁金香(char_608_acpion)、老鲤、空爆、格拉尼`。

其中 `翎羽、红豆、格拉尼` 是冲锋手的击杀回费与撤退返费，`孑、乌有` 是行商的持续扣费与零返费，其他交互包括先锋技能回费、按攻击/击杀回费、部署费用降低、按初始费用筛选、费用交换、首次免费部署、佣兵装备应变耗费和老鲤的特性耗费。

## 敌人与装置对费用的反向交互

从当前数据包的敌方图鉴中还检出一组会改变我方费用的敌人或装置。它们不是干员适配问题，但会直接改变 `battle.cost` 的输入，必须由敌人事件层接入：

- 诺伯特平民／诺伯特暗探：确认身份后撤回保护目标，返还我方费用上限。
- 萨卡兹悖谬谍报官／谍报长：携带弹药时攻击会扣除我方费用。
- 荒原窃盗者、荒原劫掠者、赏金猎人弩手：攻击达到条件后偷取我方 1 或 3 点费用，弩手按命中目标数计算。
- 赏金猎人扰乱者／队长：按在场数量降低我方费用上限。
- 萨卡兹术师结晶：阶段性封锁费用自然回复；静滞虚幻还会封锁待部署区费用最高的干员。
- 饷事：进入目标点时提供部署费用。
- “墓碑”：全场费用回复速度减半，并使再部署时间加倍；这是本期目录中已出现的费用型敌人效果。
- 全自动划账器、啸叫音响等装置：周期性消耗部署费用或在待部署区持续消耗费用，属于装置／敌方实体账本。

这些条目来自全量 PRTS 数据，是否出现在某一轮由该轮的 `enemyDbRefs`、波次和地图配置决定；没有进入下半期目录的条目不应被默认加入本期战斗。

## 模组、盟约和模式系统的费用边界

- 模组会改变基础费用、实时费用、返费倍率／上限、再部署时间或特性耗费；例如冲锋手的 CHG-X、处决者的 EXE-X、诗怀雅的 ISW 模组都不能只读取干员基础 `cost`。
- 盟约的“绝技”只改技力消耗，不改部署费用；“助力”等盟约改再部署时间，也不应写入 `battle.cost`。
- 整备阶段的 `funds`、盟约触发的资金奖励／商店价格、战斗阶段的 `cost`、诗怀雅金币和佣兵弹药必须保持四个独立资源。`SERVER_ADD_BOND_ROUND_COIN_COST` 统计的是整备资金支出，不是战斗费用。
- 战术点、流形、风雪之眼、香槟炸弹等 token 有自己的部署／刷新／撤离生命周期；其中“0 费实体”不代表给主干员免除部署费用，也不应借用干员的返费记录。

## 系统级交互

1. 关卡费用状态：初始费用、自然回复、回复速度、上限、下限和负费用。
2. 干员部署：动态部署费用计算、费用不足不能部署、首次部署/自动部署/投递部署的可返费标记。
3. 撤退与再部署：实例本次部署费用、返费倍率、返费上限、行商零返费、冲锋手返费、再部署惩罚倍率。
4. 费用事件：部署前、部署后、技能开启、攻击命中、击杀、持续耗费、撤退返费、费用比较和费用下限检查。
5. 模式资源：卫戍协议的整备资金、战斗费用、金币和收藏品奖励分属不同账本；`SERVER_ADD_BOND_ROUND_COIN_COST` 等整备规则统计的是本回合资金支出，不是战斗费用。

## 当前实现边界

当前引擎已有独立 `battle.s.cost`，战斗从 20 费用开始，并提供 `gainCost()` / `spendCost()`；费用有独立的自然回复、上限/下限、回复倍率、再部署消耗和再部署惩罚字段。行商持续扣费、费用不足撤退、诗怀雅金币和费用免死已有局部接入。

仍未完成的公共能力：

- 负费用透支和“是否考虑负数费用”的特殊判定。
- 完整动态部署费用属性与实时减费/换费；当前只覆盖基础部署费用、砾自身减费和野鬃的近卫减费。
- 实例级返费和各类可返费/不可返费标记；当前只记录再部署费用和再部署惩罚。
- 冲锋手、伊内丝、凛御银灰、缪尔赛思等费用条件与待部署区交互。
- 按攻击、命中、击杀、装备应变触发的全量回费。
- 佣兵装备耗费、老鲤特性耗费和裁度持续扣费。
- 敌方偷费、费用上限压制、费用回复封锁／减速、保护目标返费和费用型装置事件。

在这些公共能力完成前，任何“技能可解析”或“能获得几费”的测试都不能视为完整费用还原。
