# 秘术师（mystic）行为补全方案

对照来源：[深靛 · PRTS](https://m.prts.wiki/w/%E6%B7%B1%E9%9D%9B) 的特性与分支信息。本模式内秘术师只有一名干员：**深靛**（`char_469_indigo`，条目 `chess_char_1_17_a`，还有精锐 `_b` 形态）。

## 一、原作口径（PRTS 原文）

**特性**：攻击造成法术伤害，在找不到攻击目标时可以将攻击能量**储存**起来之后一齐发射（最多 3 个）。

**分支信息**（这四条是行为判定的关键）：

1. 可对空。
2. **能量储存与攻击占用相同的攻击间隔**：进行攻击判定时，若范围内存在有效目标 → 普通攻击；若无有效目标且能量未满 → 改为**储存一份攻击能量（属于攻击行为）**；仅当没有有效目标且能量已满时才进入待机。
3. **攻击被打断且弹道未能成功生成时，储存的能量不会被消耗。**
4. **由储存能量形成的弹道造成攻击力 100% 的法术普通伤害。**

深靛额外两条：

- 天赋**柔光缚目**：攻击时有 12%（精英 1）／18%（精英 2）几率使目标束缚 4 秒；**不以束缚状态的敌人为攻击目标**；※**每个攻击能量独立计算触发概率**。
- 技能 1**灯塔守卫者**备注：该技能的「每次攻击的攻击倍率」会**实时应用在特性积攒的攻击能量抛射物上**。

## 二、现状与缺口

已有实现（`native-branches.js` 的 `mystic:{damageType:'arts',antiAir:true,storage:true}`，`native-battle.js` 的 charge 动作与释放逻辑）：

| 口径 | 现状 | 判断 |
| --- | --- | --- |
| 可对空 | `antiAir:true` | ✅ |
| 无有效目标才储能 | `behavior.storage&&!targets.length&&energy<times` | ✅ |
| 储能占用一个攻击间隔 | `u.attackCooldown=t.frames` + `action.left=windupFrames`，完成后 `energy+1` | ✅ |
| 最多 3 个 | `branchTrait(p).values.times`（数据 `times:3`） | ✅ |
| 能量满后待机 | 条件里 `energy<times` | ✅ |
| 打到目标后一齐发射 | `storedEnergy` → `hit===0` 时补发 N 条弹道 | ✅ |
| 每条弹道 100% 攻击力 | `amount: packet.baseAmount`（即 `stats.atk`） | ✅ |
| **技能倍率实时作用在能量弹道上** | 能量弹道走 `packet.baseAmount`，**不带技能倍率**；主攻击走 `action.amount`（含倍率） | ❌ 缺口 1 |
| **天赋柔光缚目（束缚 + 触发几率）** | 全代码没有深靛的专属分支，`prob/duration` 原表有值但无人读取；`root` 状态本身可用 | ❌ 缺口 2 |
| **不以束缚状态的敌人为攻击目标** | `targets()` 里的规则正则写的是 `/不以束缚状态的敌人为攻击目标/`，而深靛天赋文案是「**不以**束缚状态的敌人为攻击目标」，**匹配不上** | ❌ 缺口 3 |
| 攻击被打断时能量不消耗 | 能量只在 `releaseNativeAttack` 成功后扣减，天然满足；但没有定向测试 | ⚠️ 未固化 |
| 技能 2 光影迷宫：束缚目标每 0.5 秒受 12%~20% 法术伤害（持续伤害、不触发天赋） | 走通用周期区域分支（`requiresStatus:'root'`）可能已命中，但未验证倍率取自哪个黑板键 | ⚠️ 待核对 |

**量化后果**（以技能 1 为例）：技能 1 的 `attack@atk_scale` 是 0.35~0.50，主攻击按 35%~50% 攻击力结算，但满能量释放时额外的那 3 条弹道仍是 **100% 攻击力**，实际单次爆发是 `0.35 + 3×1.00 = 3.35` 倍攻击力，而原作应为 `0.35 + 3×(1.00×0.35) = 1.40` 倍 —— **高估约 2.4 倍**。

## 二·补 实机复现：深靛与隐现「完全不攻击」（已修）

补完前先复现了用户报的现象，根因不在秘术师本身，而在 `targets()` 的**索敌规则被当成硬过滤**：

- `native-battle.js` 原本对 `cfg.targetRule` / `talentTargetRule` 一律 `targets=targets.filter(...)`。一旦过滤后一个都不剩，候选就变成空数组，干员**永远找不到目标**。
- 深靛的天赋文案命中 `rooted` 规则（「不以束缚状态的敌人为攻击目标」），场上没有束缚目标时 → 候选清空 → 不攻击，能量也不再消耗（因为一直在充能）。
- 隐现的技能 2 文案「**优先**攻击使用远程武器的敌人」命中 `ranged` 规则（`skillConfig` 用的是 `/远程武器/`，不是 `/远程/`），场上只有近战敌人时 → 候选清空 → 不攻击。

这类描述都是「优先」，不是「只能」。修法：新增 `prefer(predicate)`，过滤后为空则保留原目标集。已覆盖 `blocked` / `unblocked` / `ranged` / `air` / `lowHp` / `rooted` 六条规则。

回归测试：`tests/native-target-rule.test.mjs`（深靛无束缚目标时照常攻击、有束缚目标时优先打束缚的、隐现桌上只有近战敌人时照常攻击）。

## 三、补全方案

### 1. 能量弹道带上技能倍率（缺口 1，优先）

`native-battle.js` 里 `deliverStrike` 构造能量弹道时，把倍率一并算进去：

- 现在：`amount: packet.baseAmount ?? packet.amount`
- 改为：释放时按当时的技能倍率计算一次，例如把「技能攻击倍率」随 strike 包一起下发（`packet.skillScale`），能量弹道用 `stats.atk * skillScale`；
- 这样能同时满足「实时应用」——倍率取释放那一刻的数值，而不是蓄能时的。

顺带核对：主攻击的 `action.amount` 已经含倍率，不要重复乘。

### 2. 接入天赋柔光缚目（缺口 2）

按「每个攻击能量独立计算」的口径实现：

- 数据取 `p.activeTalents` 里 `名称==='柔光缚目'` 的 `blackboard.prob` / `blackboard.duration`（当前 0.12/4，潜能与模组会提高 prob）；
- 触发时机：**每条命中的弹道各掷一次**（主攻击 + 每条能量弹道），不是一次攻击掷一次；
- 命中的目标施加 `root`，时长 4 秒，`resistible:false`（原表未标可抵抗）；
- 技能 2 的 `talent_scale`（1.5×/2.0×/2.5×/3.0×）要把 `prob` 乘以该系数；技能 2 的额外持续伤害**不触发天赋**，需要在伤害来源上区分（`cause` 标记）。

### 3. 修掉「不以束缚状态的敌人为攻击目标」的匹配（缺口 3）

`native-battle.js` 里 `talentTargetRule` 的正则与实际文案不一致，导致深靛会继续攻击已被束缚的目标。方案：改成正则 `/(不以|不会以).*束缚状态的敌人为攻击目标/`，或按天赋名「柔光缚目」精确匹配。两条都行，选后者更稳（不受文案空格/标点影响）。

### 4. 能量在重新部署时清零（缺口 4）

`deploy(u,{reentry:true})` 的重置清单里补 `u.energy=0`，避免再部署后带着上一轮的能量进场。这条要确认原作为「再部署后能量清零」，我按惯例保守处理：清空并在文档里标注为**待核**。

### 5. 固化「打断不消耗能量」（缺口 5）

不改代码，补定向测试：让深靛在释放前被打断（例如释放瞬间目标死亡或自身被晕眩），断言 `u.energy` 没有被扣。

### 6. 核对技能 2 的持续伤害（缺口 6）

验证「束缚目标每 0.5 秒受 X% 攻击力法术伤害」是否落到通用周期区域分支上、倍率取的是 `indigo_s_2[damage].atk_scale`（0.12~0.20）、间隔取 `indigo_s_2[damage].interval`（0.5）；以及**伤害分类是法术持续伤害、不触发天赋**。若走的是通用分支但取值不对，就在 `native-operator-effects.js` 里补一条专属分支。

## 四、验收口径

1. 固定种子下：满能量释放时，总伤害 = 主攻击（含技能倍率）+ 3 × 100% 攻击力 × 技能倍率；技能 1 下两者按同一倍率缩放。
2. 天赋：每条弹道独立掷概率；`prob` 取当前养成状态的值；技能 2 的 `talent_scale` 生效；技能 2 的持续伤害不触发天赋。
3. 已束缚的敌人不再被深靛选为目标。
4. 能量上限 3、无目标才积累、释放后清零、打断不消耗、再部署清零。
5. 全量测试通过，`dist/native.bundle.js` 重建。

## 五、本批不做

- 秘术师其他成员（爱丽丝、和弦、戴菲恩、黑键）不在本模式干员表里，本批不引入。她们的差异点（爱丽丝「储存的攻击能量造成的伤害提升至 115%~134%」）说明「单份能量伤害倍率」在原表里是由天赋改写的——如果以后要纳入，需要把第 1 步的倍率做成可被天赋覆盖的字段，而不是写死 100%。
- 模组与潜能的数值差异（`requiredPotentialRank`）由既有的养成状态解析负责，本批不新增解析。
