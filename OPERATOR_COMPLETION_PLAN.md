# 盟约下半期 112 名干员适配目标

范围固定为 2026-03-27 更新后的盟约下半期名单：112 名干员、283 个技能条目。目标是逐名完成所有适用技能、天赋、模组、自动作战策略和附属实体；本期不适用的机制单独记录，不用通用近似冒充完成。

## 分阶段目标

| 阶段 | 目标 | 通过条件 | 状态 |
| --- | --- | --- | --- |
| S1 | 全名单数据和适配入口 | 112 名均有 adapter；283 个技能可安全解析；固定资料版本一致 | 已完成 |
| S2 | 事件、时间轴和目标系统 | 伤害/治疗/流失/死亡/状态/技能事件统一；索敌、前摇、多段、技能策略可追溯 | 基础版已接入，待逐项校准 |
| S3 | 状态、位移和区域系统 | 恐惧、战栗、隐匿、浮空、推拉、传送、区域进出可序列化并复验 | 公共基础已接入，待逐名校准 |
| S4 | 元素、资源和防护系统 | 元素损伤、爆发、战斗费用/整备资金/金币/弹药/充能、伤害转移/延后、复活规则可独立测试 | 公共基础已接入，战斗费用与整备资金已分离，待逐名校准 |
| S5 | 通用实体系统 | token、召唤、陷阱、装置、浮游单元、替身和主人生命周期统一建模 | token 目录、主人生命周期、召唤与香槟炸弹陷阱已接入，待逐名扩展 |
| S6 | 逐名专属适配 | 112 名每人的技能、天赋、模组和模式例外均有真实场景；`needsSpecialHandler` 清零或有明确 exception | 已完成：112 名均有局部专属处理器，`needsSpecialHandler=0` |
| S7 | 可玩性对照验收 | 固定种子下机制结果、索敌、长局、暂停恢复和移动端检查通过；动画前后摇与视觉预制体另列延后项 | 待开始 |

## 当前基线

`descriptor-v1` 适配层已覆盖通用黑板参数、直接属性、光环、概率事件、基础状态、资源和持续效果入口。公共结算层现在提供 `queueDelayedDamage`、`addDamageRedirect`、`teleportActor`、区域进出事件和 `reviveActor`，并保留快照字段以支持暂停恢复；逐名处理器覆盖沉睡、元素损伤、弹药、炼金区域、浮游单元、召唤、位移、反伤、复活和费用联动。`operator-adapter-manifest.json` 记录每名干员的适配状态；`needsSpecialHandler=0` 表示 112 名均已进入逐名处理器，具体边界记录在审计条目的 `boundary` 字段。

当前验证：`npm test` 308 项通过；敌人行为策略、攻击附加效果、死亡爆炸、低生命阶段和随机池过滤均有定向测试，所有 112 名干员和 283 个技能完成无异常 smoke。适配清单记录 112 名干员均有局部专属处理器，`needsSpecialHandler` 为 0；仍需在 S7 用固定种子做机制结果和索敌行为对照，动画前后摇与视觉预制体不属于本阶段阻塞项。

## 执行规则

- 每个阶段先补公共系统，再按等阶逐名接入；同一机制只保留一个公共实现。
- 每名干员至少有技能开启、命中/治疗、技能结束、击倒/再部署中的适用场景。
- 只在真实场景通过后更新 `operator-capability-status.json`；构建脚本不自动升级 `verified`。
- 每阶段独立提交，提交前运行全套测试和必要的浏览器检查；未通过不合并 `main`。
- 所有暂定数值、未取得的动作帧和模式外能力继续标注来源与边界。
