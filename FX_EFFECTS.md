# 战斗特效覆盖

2026-09-14：按已有攻击行为扩展通用特效，不改变伤害数值或结算时间。实现位于 `dist/native-fx.js`，事件来自 `dist/native-battle.js`。

| 类别 | 接入依据与表现 |
| --- | --- |
| 挥砍／突刺／重击 | 按分支选择弧形刀光、箭头刺击、放射冲击，跟随实际 strike 事件 |
| 群体挥扫／散射／范围术式 | 每个实际释放目标分别反馈；散射手增加扇形射线，范围术式在目标位置绘制印记 |
| 范围扩大技能 | 技能 `rangeId` 与干员常态 `rangeId` 不同即为「范围扩大」，形状取自 `data.ranges[rangeId].grids`（`protocol.js` 的 `rangeGeometry`），不靠像素半径估算。分三种画法：**持续范围强化**（真银斩、序时之匙等，`wideKind==='sweep'`）开技时大弧线扫过整个范围、之后每次挥砍沿范围扫弧；**瞬时自身 AoE**（剑雨、药物配置等，`burst`）与**入场自动释放**的被动（缄默德克萨斯 S2/S3，`passive`）以自身为中心画扩散震波，近身范围画圆环、大范围按格描边；**持续领域**（钙质化、狐火渺然等）在技能持续期内按格描出范围边界并低频脉动。判定仍走 `inside`/`range`，特效只读 `wide`/`wideKind` 标记 |
| 区域／领域效果 | 逻辑层的 `s.logicEffects`（`kind:'zone'`，带 `x/y/radius/trackArea/values`）之前完全没有画面；现在由 `drawZones` 统一渲染：按 `zoneVisual(talentOrSkillId,values)` 取形状与色调，圆形按 Chebyshev 半径铺格、`self` 型只画施法者一格；周期结算前 0.25 秒补一圈脉冲，剩余不足 1.5 秒开始闪烁提示结束。覆盖雷暴（辉煌裂片）、斩击领域（归于宁静）、金色领域（钙质化）、全场结界（防火墙）、水涡（涌潮悲歌）、沙尘、灼燃／寒冷／束缚领域与元素回复光环等 15 类。色调表在 `native-operator-effects.js` 的 `ZONE_VISUALS`，未登记的按 `values` 推断 |
| 瞬时多目标 | 辉煌裂片、御敌的锋锐这类技能走 `dealDamage` 而不是挥砍，没有 `strike` 事件；`drawSkillFan` 用「开技后 0.3 秒内落在技能范围内的 `hit` 事件」连成扇面（弧线 + 到各目标的连线），不足 2 个目标时按普通命中处理 |
| 物理／法术弹体 | 方向拖尾与菱形；发射时仅显示枪口／施法标记，不再用长线假装立即命中 |
| 链式攻击 | 连接实际弹跳目标，不再用圆形范围表现链式伤害 |
| 普通／群体／链式治疗 | 恢复发生时绘制治疗连接和目标十字，链愈沿实际目标连接；咒愈与自恢复复用真实治疗事件 |
| 炮击 | 弹体抛物线高度为纯显示偏移，地面阴影保留逻辑落点；命中后爆炸环与碎片 |
| 余震 | 延迟结算时单独产生同中心、同半径的第二次震荡 |
| 回旋 | 旋转弧刃，按实际弹体往返位置绘制 |
| 浮游单元／储能 | 在场单位的浮游标记、实际额外弹体与储能数量标记 |
| 装填 | 弹仓数量和装填阶段轮廓 |
| 持续回复 | 已有持续回复分支的范围地块、光环与十字标识 |
| 谢拉格寒风 | 6 人谢拉格盟约每 25 秒结算一次全场冰风；结算时全屏铺一层白色偏冰蓝的冷雾并横扫数道风痕，1 秒渐入渐出。只提亮不压暗，且绘制在 HUD 之下，血条／技力条／界面文字不受影响。事件由 `native-effects.js` 在寒风结算处发出，画法在 `native-fx.js` 的 `drawIceWind` |
| 敌方攻击 | 近战冲击与远程命中射线；目前敌方远程仍为即时命中逻辑，不虚构飞行延迟 |

保留技能开关、部署、控制、倒地、漏怪及音量功能。减少动效模式关闭旋转、弹体高度、碎片和额外散射射线，保留目标与类别提示。动画仅依赖模拟时间，暂停与倍速保持同步。冰风在减少动效模式下把风痕从 8 道降到 3 道、冷雾更淡，但仍保留可见提示，因为寒冷本身带有攻速与谢拉格增伤等机制含义。冰风没有声音反馈。

验证：69 项针对性回归通过；`scripts/combat-acceptance.cjs` 的 8 项浏览器检查通过；`scripts/fx-preview.cjs` 生成 15 组预览并验证渲染无状态修改、无浏览器异常。预览图位于 `artifacts/fx-preview/categories.png`。谢拉格寒风由 `tests/native-fx-ice-wind.test.mjs` 覆盖：25 秒结算发出一条事件、不足 6 人不触发、窗口内渐入渐出、窗口外不绘制、减少动效降档，以及绘制不写入战斗状态。

荒芜拉普兰德「终幕·浩劫」的浮游单元（`battle.s.whitwEyes`）是自由飞行的独立单位，之前只有逻辑没有画面。现在由 `native-fx.js` 的 `drawWhitwEyes` 给每个单元画一枚浪头素材：约 0.86×0.5 个地块大小，一弯白色卷浪加数点浪花，朝速度方向淡出的尾迹用来看出实时移动；攻击瞬间补一圈涟漪。减少动效模式下整体缩到一半并降透明度。该绘制只读 `x/y/vx/vy/nextAttackAt`，不写战斗状态。

炎盟约 6 人的召唤物「炎佑」（`type:'yan-guardian'`）走自己的攻击循环，不会发通用的 `attack`/`strike` 事件，因此 `yanDamage` 在普攻时补发一条 `yan-bolt`（带炎佑与目标坐标，只为表现层服务，不参与结算）。`drawCombatFx` 据此画普攻：`(255,244,214)→(255,146,74)` 的灼燃弹体带拖尾飞向目标，命中前 28% 的行程炸开一圈橙色冲击环加一圈元素脆弱紫环；「祛恶之焰」持续期内（`yanSkillActive`，20 秒）在炎佑身上画三圈旋转火轮、一圈淡环和上浮火星，同时给当前锁定目标套一圈淡橙指示环。减少动效模式缩到 0.62 并把火星从 5 颗降到 2 颗。

边界：为通用程序化效果，非原作美术逐帧复刻。专属技能素材、召唤和变身，以及尚未实现的持续灼烧／元素损伤，不因已有特效分类而视为完成。链式连接与当前同步伤害结算一致，不额外引入跳转伤害延迟。
