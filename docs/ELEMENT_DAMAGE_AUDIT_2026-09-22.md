# 元素损伤 / 元素伤害 审计（2026-09-22）

起因：用户报「烛煌的攻击不知道为什么能累计灼烧，应当没有这个默认能力」。
结论：**两条通用兜底都把「普通数值键」当成了元素比例**，烛煌只是其中一个受害者（焰影苇草更严重）。
本页记录代码落点、逐条核对结果、实测数据与仍未闭环的清单。

## 一、两个概念别混

| 概念 | 代码 | 说明 |
| --- | --- | --- |
| **元素损伤**（损伤条） | `native-effects.applyElementDamage` → `target.elemental[type]` | 累积到上限才爆发：`neural`／`burn`／`corrosion`／`necrosis`。上限 `elementalMax`＝敌人 1000、BOSS 与木桩 2000、干员 1000。**同时只保留一种**（同帧内按批次取最大的那一类，之后换类型会被判 `immune`）。爆发期间**锁条**（`elementBurstUntil > time` 直接返回）。 |
| **元素伤害**（伤害类型） | `type:'elemental'` → `combat.js damage()` | 按目标的 `elementResistance` 减免（下限为原值的 5%）。与「真实伤害」`type:'true'` 是两回事，别互相顶替。 |

爆发结算 `settleElementBurst`（`native-effects.js`）：

| 类型 | 对敌人 | 对我方干员 |
| --- | --- | --- |
| `neural` 神经 | 3 层麻痹（5 秒衰减）＋ 6000 元素伤害 | 眩晕 10 秒 ＋ 1000 真实伤害 |
| `burn` 灼燃 | 法术抗性 −20（10 秒）＋ 7000 元素伤害 | 1200 法术伤害 |
| `corrosion` 腐蚀 | 防御 −120 ＋ 5000 元素伤害 | 800 物理伤害 ＋ 自身腐蚀累积 100 |
| `necrosis` 凋亡 | 攻击力 −50%（15 秒）＋ 每秒 800 元素伤害的持续伤害 | 技能封锁 ＋ 每秒 100 法术伤害与 1 点技力流失 |

爆发窗口 `elementBurstUntil`：`neural`／`burn` 10 秒、`necrosis` 15 秒、`corrosion` 敌人 8 秒／我方 10 秒。
`native-effects` 在爆发时额外记 **`target.elementBurstType`**（本次爆发类型），因为条件式技能要问「目标是不是正在**灼燃**爆发」（烛煌 S3），只看 `elementBurst` 布尔值判不出来。

## 二、根因：两条兜底都把普通数值键当元素比例

1. **技能版**（`native-operator-effects.operatorSkillStart` 的 after-damage 分支）
   原来写成「技能文案里出现 灼燃／凋亡／神经损伤／元素伤害 → 就按 `atkScale` 给**每次攻击**挂元素」。
   烛煌 S2「沸血燎原」的元素属于**灼烧地段**（敌人经过／站在上面每秒结算）、S3「众恶的焚场」的元素只在**目标处于灼燃爆发期间**追加，两者都不是普通攻击的附属效果——于是她不开技也在累计灼燃。
   → 现在收窄为白名单 `GENERIC_ELEMENT_SKILLS`（**当前为空**：文案提到元素的技能都有专属实现），数值只认原表显式的 `elementScale`／`ep_damage_ratio`／`attack@ep_damage_ratio`；已核对过的干员**不得**进表。
2. **天赋版**（同一个 after-damage 分支里）
   原来写成 `bb.ep_damage_ratio ?? bb.element_damage_scale ?? bb.damage_scale ?? bb.elementScale`。
   焰影苇草天赋「灼痕」的黑板是 `damage_scale: 1.15`——那是**【法术脆弱】的承伤倍率**，被当成了灼燃比例，于是她**每次攻击**都挂 115% 攻击力的灼燃（实测一次攻击 572 点，比烛煌还高）。
   → 现在只认 `ep_damage_ratio`／`element_damage_scale`。烛煌天赋的 `ep_damage_scale` 也**不能**进这张通用表：她的「熔点引爆」是爆发时的专属结算。

## 三、烛煌（`char_1040_blaze2`，本源术师）逐条核对

本源术师的分支特性写「攻击造成法术伤害，可以造成元素伤害」——那是**许可**，不是「每次攻击都附带元素」。

| 来源 | PRTS 文案 | 实现 |
| --- | --- | --- |
| S1 炙手之援 | 立即选择攻击范围内生命上限最高的一名干员，每秒对其周围所有敌人造成攻击力 46% 的法术伤害和**相当于法术伤害 30%** 的灼燃损伤，持续 20 秒 | 圈 `blaze2-s1`：圆心在生命上限最高的友军身上，半径 1.5 的圆（`shape:'circle'`）、1 秒一跳、`groundOnly`（只吃地面敌人）。元素量＝**该次法术伤害 × 30%**（`values.elementOffDamage`）。 |
| S2 沸血燎原 | 攻击力 +90%、同时攻击 3 名敌人；攻击时流失 5% 生命并在目标所在位置留下灼烧地段：经过的敌人移速 −50%，每秒受到烛煌攻击力 35% 的法术伤害和**相当于法术伤害 30%** 的灼燃损伤 | 攻击**本身不挂元素**；每次攻击在目标脚下放 `blaze2-s2-<目标uid>`（半径 1.5、8 秒、1 秒一跳、带减速）。元素量同上按该跳的法术伤害算。 |
| S3 众恶的焚场 | 攻击范围改变、攻击力 +70%、攻击间隔大幅缩短、群体攻击；**若目标处于灼燃损伤爆发期间**则额外造成攻击力 60% 的**元素伤害** | 18 发弹药（`attack@trigger_time`）。普通攻击**不挂元素**；只有 `elementBurstActive(target,'burn',time)` 为真时才追加 `atk × 0.6` 的 `type:'elemental'` 伤害（`blaze2-s3-burst:*`）。 |
| 天赋 熔点引爆 | 全场有敌人**灼燃损伤爆发**开始时，立即对其造成相当于攻击力 250% 的**元素伤害**并回复 6% 最大生命（潜力档越高数值越高） | **本次修好**：旧实现遍历对象被过滤成「只遍历妮芙」，`u.id==='char_1040_blaze2'` 永远进不来，天赋一次都没触发过。现在按 `payload.element==='burn'` ＋ 目标是敌人才结算；伤害走 `elemental`（不是真实伤害），回复按 `hp_ratio`；S3 弹药型技能期间再按技能黑板的 `ammo_recover` 补 2 发。 |
| 天赋 绝处重燃 | 受到致命伤害时…… | **已实现**：致命伤后 `downed`（留在场上、6000 屏障、`healable=false`、每秒回 3% 生命上限），回满后复活并眩晕附近敌人（`tests/native-element-misc.test.mjs`）。 |

## 四、实测（同一探针，改前／改后）

| 场景 | 改前 | 改后 |
| --- | --- | --- |
| 烛煌常态 6 次攻击 | 每次按 `atkScale` 累计灼燃 | **0**（一条元素都不挂） |
| 烛煌 S3 施放中 6 次攻击 | 每次 `atk × 0.6` | **0** |
| 烛煌 S2 施放中 6 次攻击 | 每次 `atk × 0.35` | **0**；灼烧地段每秒 110.92（＝攻击力 1056.4 × 35% × 30%） |
| 烛煌 S1 圈每秒 | 攻击力 × 30% ＝ 166.8 | 该跳法术伤害 × 30% ＝ 76.73 |
| 烛煌 S3 对灼燃爆发中的目标一次攻击 | 追加 567.12（其实也有，但没区分条件） | 追加 567.12（＝攻击力 945.2 × 60%），非爆发期 0 |
| 烛煌 熔点引爆（敌人灼燃爆发） | **0**（死代码） | 元素伤害 1946（＝攻击力 556 × 3.5）＋ 自回血 144.72（＝1206 × 12%）＋ S3 期间弹药 +2 |
| 焰影苇草一次攻击 | 572 灼燃（＝攻击力 × 1.15） | **0**；「灼痕」标记状态照旧 |
| 塑心／盟约·辅助干员一次攻击 | 按 `ep_damage_ratio` 挂凋亡 | 不变（通用天赋通道的既有用户没被误伤） |

## 五、全表审计：文案提到元素的干员只有 9 名

用 `NATIVE_DATA.profiles` 遍历全部 260 个形态（112 名干员）的技能与天赋文案，命中 `灼燃|凋亡|神经损伤|腐蚀|元素伤害|元素损伤|灼痕` 的只有下面 9 名：

| 干员 | 元素出现在 | 现状 |
| --- | --- | --- |
| 烛煌 `char_1040_blaze2` | S1/S2 区域、S3 条件、天赋爆发 | 本次修复（见上） |
| 焰影苇草 `char_1020_reed2` | 天赋「灼痕」 | 已闭环：「灼痕」按 30% 概率施加（攻击力 −10%、法术脆弱 15%、6 秒），S3 期间无限持续、技能结束清空 |
| 妮芙 `char_4146_nymph` | S1/S2/S3 攻击附带凋亡；天赋「失魂」「窥心钥」；模组 ALC-X | 已闭环：S1/S2/S3 专属（S3 在爆发期改元素伤害）、「失魂」每秒元素伤害、「窥心钥」按爆发次数叠攻击力、模组对爆发期敌人 ×1.1 |
| 塑心 `char_245_cello` | S1/S2 附带凋亡；天赋「无词哀歌」「精神逆构」 | S1/S2 专属；两个天赋都是专属每秒 tick（不进通用天赋通道） |
| 菲莱 `char_4148_philae` | S1 清除损伤＋损伤屏障；S2 受击反伤附带凋亡；天赋 | S1/S2 专属（`philae-s1`／受击反伤）；元素条被打满时回 2 技力；「神河谕使」的元素损伤 −10%／−12% 按黑板赋值 |
| 余 `char_2026_yu` | S1 受击挂灼燃；S3 火墙；天赋「礼尚往来」「闲云隐市」 | 已闭环：S1 实测正确（548 × 30% ＝ 164.4）；S3 火墙只留视觉、规则是「友方法术穿墙附带灼燃」＋「敌方子弹 10% 被拦」；「礼尚往来」每秒 40% 法术＋12% 灼燃 |
| 纯烬艾雅法拉 `char_1016_agoat2` | S1/S2/S3 与两个天赋全是**元素损伤回复** | 已逐条接线：S1 每秒 4%（无视禁疗）、S2 损伤屏障、S3 治疗＋元素回复各 5 连发、天赋光环 12% 与生命上限 +6% |
| 哈洛德 `char_4114_harold` | S2 优先治疗损伤最重的目标＋回复量提升；天赋降低受到的损伤 | 已闭环：优先选目标、`trait_scale` 回复提升、`ep_damage_resistance` 12%（文案「超过一半」才加过半条件，已加门禁） |
| 盟约·辅助干员 `char_616_pithst` | 天赋「迭代元素」三种损伤 | 走通用天赋通道，数值＝攻击力 × 18%，**类型**按文案「神经损伤（优先）」判定 |

## 六、回归与门禁

`tests/native-operator-effects.test.mjs` 新增 4 条：

- 天赋元素比例只认显式元素键（源码门禁）＋ 焰影苇草一次攻击零元素、「灼痕」标记照旧 ＋ 塑心／盟约·辅助干员仍生效。
- 烛煌常态／S2／S3 攻击零元素、元素只来自区域与 S3 条件分支（含源码门禁：通用兜底必须走白名单、已核对干员不得进表）。
- 烛煌 S3 的额外元素伤害只在灼燃爆发期间结算（按 `attack@atk_scale` 取期望值）。
- 烛煌「熔点引爆」：灼燃爆发造成元素伤害并自回血、S3 期间补弹药、**凋亡爆发不触发**。

第二轮回填新增的回归文件（口径见第七节）：

- `tests/native-element-rules.test.mjs`（4 条）：妮芙损伤量／S1 爆发追加／S3 改元素类型、塑心每秒凋亡＋类型专属加成、
  焰影苇草 30% 门槛与 S3 清空、**妮芙模组 ALC-X 的 110% 倍率**（普攻与「失魂」两条路径）。
- `tests/native-element-support.test.mjs`（6 条）：纯烬 S1/S2/S3 与「火山灰疗愈」、哈洛德 S2 与「我即军营」的
  12%（含「文案写超过一半才过半」这条判定）。
- `tests/native-element-misc.test.mjs`（5 条）：视界、炼金单元模组技力、凯瑟琳 S2 停止攻击、烛煌绝处重燃、迭代元素类型。
- `tests/native-thorn2-alchemy.test.mjs` / `tests/native-alchemy-unit.test.mjs`：引星棘刺与锡人的炼金单元口径
  （区域几何、无来源伤害、生命回复速度）。

## 七、遗留项结算（2026-09-22 第二轮回填）

本页原来列的 10 条待办已全部落地，逐条对照如下（口径与数值都写进了各自的回归）：

1. **妮芙 S3「心防溃决」**：`native-battle.hit()` 在 `elementBurstActive(e,'necrosis')` 为真时把这一击的伤害类型
   换成 `type:'elemental'`（按 `elementResistance` 减免），不再是「技能期间每次攻击都挂凋亡」。
2. **妮芙天赋「失魂」**：按 `element_atk_scale`（40%，S2 命中时 70%）对**处于凋亡爆发期间**的敌人每秒结算
   攻击力该比例的元素伤害，爆发结束即撤掉标记（`e.nymphSoul` 记 `sourceUid/scale/until`）。
3. **塑心天赋「无词哀歌」**：独立的每秒 tick（`celloTalentNextAt`）对攻击范围内敌人挂凋亡损伤攻击力 6%＋停顿 0.2 秒；
   「精神逆构」按 `ep_damage_scale` 只提高**凋亡**损伤（`elementDamageTakenBonusByType`）。
   专属天赋干员名单是 `TALENT_ELEMENT_DEDICATED`，通用天赋通道必须跳过它们。
4. **盟约·辅助干员「迭代元素」的类型优先级**：通用通道改成优先匹配文案里的「神经损伤（优先）」，实测挂 `neural`。
5. **余 S3「灶里乾坤」火墙**：技能只留视觉（`values.firewall`），规则分散在三处——友方法术伤害穿过火墙时按
   `elementRatio` 附带灼燃（`firewallElementBurn`，只认 `type==='arts'`、非 `dot`、有来源）、
   敌方**子弹**穿过火墙时按 `prob` 被拦下（`native-enemy-attacks.clearedByFirewall`，速度从 `dist/(impactAt-startedAt)` 推）。
6. **余天赋「礼尚往来」**：每秒 tick 对**被自己阻挡**的敌人造成攻击力 40% 法术伤害＋12% 攻击力灼燃损伤。
7. **元素损伤降低类**：哈洛德的「我即军营」走通用扫描（`ep_damage_resistance`＋文案「元素损伤…降低」，
   **文案写「超过一半／超过半数」时才加过半条件**）、菲莱的「神河谕使」是专属赋值
   （`damage_resistance` 5/7/10/12%），纯烬的「火山灰疗愈」是范围内光环（12%，无过半条件）。
   回归：`tests/native-element-support.test.mjs` 的两条 12%（有／无过半条件）。
8. **元素损伤回复**：纯烬艾雅法拉 S1（每秒 4%，无视禁疗）、S2（损伤屏障，`battle.s.elementBarriers`）、
   S3（治疗＋元素损伤各 5 连发，优先不同目标）、哈洛德 S2（过半时 `trait_scale` 倍）全部接线；
   `healElements(source,target,amount,{ignoreHealingBlock})` 是唯一入口并返回实际回复量。
9. **焰影苇草「灼痕」**：30% 概率门（`reed2Talent` 标记），数值取当前档黑板（攻击力 −10%、15% 法术脆弱、6 秒），
   S3 期间施加的无限持续、技能结束整批清空并重算脆弱。
10. **烛煌「绝处重燃」**：致命伤 → `downed`（仍留在场上、6000 屏障、`healable=false`、每秒回 3% 生命上限），
    回满后复活并眩晕范围内敌人。
11. **妮芙模组 ALC-X**（本页之外新增的一条）：`NativeBattle.moduleElementBurstScale` 让「对处于元素爆发期间的敌人
    造成的伤害提升至 110%」在 `native-effects.dealDamage` 统一乘算——普攻、技能、天赋持续伤害（「失魂」）
    都会吃到，不要在技能结算里再乘一次。回归：`tests/native-element-rules.test.mjs` 第 4 条。
12. **锡人的炼金单元**（同一次排查发现）：伤害改成无来源（`source:null` + `creditUid`），
    「大拉里」的圈内回复从 `hot` 改成 `regen`（生命回复速度，不受治疗加成与禁疗影响）。

**仍未闭环**：

- **元素爆发的解除条件**逐条核对只覆盖了本期 9 名干员，敌人侧的爆发（如巢涌者神经损伤）没做数值对照。
- 元素损伤的**显示**只有棋盘上的条与头顶角标，没有原作的损伤爆发演出对照（画布表现只能按源码口径核对）。

