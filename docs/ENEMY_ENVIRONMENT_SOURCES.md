# 敌人环境条件来源核对

2026-09-21，基于固定历史提交 `86da4cfa3a4b958c3615fccf5afbc10b5c7f1bfb`。范围是 `data/modes/alliance-lower/levels/manifest.json` 登记的50份原始关卡，以及 `data/gamedata/allianceLower/` 的角色/技能表；不以画面、地图名字或字段简称推断环境已开启。

## 降雪与盟约寒风

| 原表对象 | 原始关卡中的引用 | 技能/依据 | 结论 |
| --- | --- | --- | --- |
| `trap_050_blizzard` 暴风雪 | 0份 | `sktok_blizzard`、`sktok_blizzard_2` | 历史角色表存在装置，不代表本期关卡生成了它 |
| `trap_1112_acblzd` 盟约寒风 | 11份地图，均有启用的预置实体 | `sktok_acblzd`；名称“谢拉格寒风”，描述为周期性对全场敌人施加寒冷，技能黑板为空、预制体`sktok_empty` | 本期专用盟约效果，与通用降雪控制器不是同一实体；不能因缩写`blzd`就自动映射为降雪 |
| `enemy_1042_frostd` 寒霜 | 多份波次引用 | 敌人实体及攻速降低光环 | 不是天气生产者 |

11份地图分别是 `level_act1autochess_m01` 至 `m07`、`level_act2autochess_m01` 至 `m04`。盟约寒风控制器位于可见棋盘裁切之外；“裁切内没有装置”也不能作为忽略全局效果的依据。

[PRTS暴风雪](https://prts.wiki/w/暴风雪)区分技能1的降雪与技能2的封冻环境：前者可依地图配置周期施加寒冷/产生定向风力，后者按我方部署时间循环寒冷与冻结。[锏敌方页](https://prts.wiki/w/锏(敌方))的两阶段降雪加攻分别为30%/50%；对应本期黑板仍保留，但不能由“敌人身上有寒冷”或“谢拉格盟约触发”直接判定降雪。

当前处理：不向本期地图凭空添加通用暴风雪，不给锏常驻加攻；谢拉格盟约寒风仍走已有盟约实现。**仍需证据**：本期专用盟约寒风是否由游戏内部额外广播通用降雪事件，以及其他本期动态机制是否会创建该事件。原始预置/技能表不能单独证明两者相通，也不能单凭缺少通用装置证明永远不相通。

这不是新增范围豁免，锏的环境条件项仍保留。后续若确认降雪事件来源，应从那个来源接入条件生命周期，不能直接修改锏的基础攻击力。

## 烟花发射器与R系列装置

第72批重新枚举50份原始关卡的 `predefines.tokenInsts`：`trap_186_cnvfwks` 烟花发射器，以及 `trap_075_bgarmn` / `trap_076_bgarms` / `trap_077_rmtarmn` / `trap_078_rmtarms` 四种未激活R装置，预置引用均为0。历史角色/技能表有这些定义，不能据此向本期地图添加实体。该检查只证明没有预置入口，不证明没有动态生成来源；动态来源与实体生命周期仍待补，不作为范围豁免。Dor回SP消费者和气球非首要目标已接入，装置吸收/激活和烟花治疗仍未完成。

## 血债账簿/复仇黑手

第74批核对历史角色 `trap_092_vgctrl`（复仇黑手）及技能 `sktok_vgctrl`：上限500、初始0、自然回复每秒3、技能持续35秒，黑板攻击+20%、防御+15%。50份关卡预置没有该控制器；这些数值未自动施加到本期地图。扎罗 `Passive.sp=1` / `WildCalling.sp=35` 是向环境账款增加值，并非自身55SP槽。已接扎罗施法及可选账款接收端，但尚无控制器初始化/自然回复/满额转清算/召唤路由；模拟接收端测试不证明真实地图上有血债效果。

## 紧急供暖器

第78批核对 `trap_137_winfire` / `sktok_winfire`，本期原始地图无此预置。敌人侧只接受存活、在场、`heaterState=lit` 的该装置实体：周围8格用于持盾者受热条件，右侧1格可部署格用于强攻冠军风门优先。当前尚无实体初始化、受击技力点燃、熄灭恢复和解除封冻链路，不能把定向测试提供的热源当作实际地图上已经存在。持盾者未受热每3秒增层不依赖环境封冻，故本期无热源时也应执行。
