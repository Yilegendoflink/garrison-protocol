# 盟约禁用（作战前"缺席盟约／被禁用干员"）机制调查

调查日期：2026-09-22。范围：原版《明日方舟》「卫戍协议：盟约」（2025-11-14 上半 / 2026-03-14 下半）。

## 零、用户口径（2026-09-22 最终确认，实现以此为准）

* **每局随机禁用 3 个核心盟约 + 4 个附加（小）盟约**，从**处于「参与随机」状态的盟约**里抽（默认 8 核心 + 10 附加共 18 个；**不看** `modeDataDict[].inactiveBondIdList` 与 `constData.trBannedBondIds`）。
* **每个盟约在「协议自定义 → 禁用方案」页三选一**（用户 2026-09-22 追加）：**固定禁用**（每局必缺席，不占 3＋4 的随机名额）／**参与随机**／**固定不被禁**。配置存在 `always`／`never` 两个数组里，`banRules` 解析、`banPool` 排除。
* **默认方案里固定不被禁的是协防干员（`emptyShip`）／绝技（`suntShip`）／调和（`maniShip`）／独行（`soloShip`）＋投资人（`investShip`）**（`BOND_BAN_EXCLUDED` ＋ `BOND_BAN_DEFAULT_NEVER`；投资人是用户 2026-09-22 追加）。注意中文名与 id 的对应关系：**助力＝`deputShip`、远见＝`visiShip`、协防干员＝`emptyShip`、投资人＝`investShip`**，不要按字面猜。
* **每个盟约各有一份「不禁用名单」（第二版口径，2026-09-22 更正）**：某个盟约被本局禁用时，挂在它名下的干员**只有出现在它的不禁用名单上**才能出场；名单之外的一律禁用——**哪怕这名干员还挂着别的没被禁的盟约**。
  * 第一版理解的「名下所有盟约都被禁才禁用」是**错的**，不要再退回去。
  * 干员身份按 `charId` 归并：精锐与初始形态共用一条名单登记。
* **限制覆盖所有获取渠道**（用户 2026-09-22 追加）：商店抽取之外，策略／道具／卫戍点名发放、晋升奖励候选、援军转让同样拿不到被禁干员。
* **道具相关的盟约不受影响**（用户 2026-09-22 追加）：装备自身的 `giveBondId` 归属照旧用于商店／具名池取货，变形同构体照旧把另一件装备的盟约借给携带者——被禁的是**干员的获取**，不是盟约本身。
* 逐盟约的 ban 影响表与"3+4 有多大杀伤"的抽样见 `BOND_BAN_RESEARCH_TABLES.md` 的附录 D／E。
* **19 个盟约的真实不禁用名单已由用户逐条给出**，落在代码的 `BOND_EXEMPT_TABLE`（按「游戏内显示名＋阶」登记，解析见 `resolveBondExempt`）；这 19 条＝18 个参与随机的盟约 ＋ 默认不被随机禁的投资人，本文件第七节是原始名单与本项目的解析结果。

下面第一～三节是这次上网调查的原始结论（官方文本证据与数据映射），第四～六节是落地实现、边界与名单原表。

## 一、机制是什么（有官方文本的三条证据）

原版每次模拟开始前会打印一份**简报**，其中两项就是本机制：

> 开始模拟后将会打印本次模拟简报，包括：
> * 本次模拟的**最终领袖敌人**及**特训敌人组合**
> * 本次模拟**可用的盟约**
> * 本次模拟中**盟约的缺席情况及被禁用干员**
> ** 禁用盟约有可能影响自选的支援干员
>
> —— [卫戍协议：盟约 下半（PRTS 活动页）](https://prts.wiki/w/%E5%8D%AB%E6%88%8D%E5%8D%8F%E8%AE%AE%EF%BC%9A%E7%9B%9F%E7%BA%A6_%E4%B8%8B%E5%8D%8A)（[上半页](https://prts.wiki/w/%E5%8D%AB%E6%88%8D%E5%8D%8F%E8%AE%AE%EF%BC%9A%E7%9B%9F%E7%BA%A6)同文）

游戏内帮助页（官方文案）把同一件事写成"战前选择"的第一句：

> ===战前选择===
> 正式进入作战前，您将事先得知本次战斗即将迎战的领袖单位，三种特训敌人的类型，以及**不完整的盟约与不会进入调配池的干员**。
> 随后所有玩家将按照顺序选择一个自己的策略。
>
> —— [卫戍协议/帮助](https://prts.wiki/w/%E5%8D%AB%E6%88%8D%E5%8D%8F%E8%AE%AE/%E5%B8%AE%E5%8A%A9)（该页已是盟约期版本）

规则来源（为什么"载入的盟约有限"）：

> ===盟约===
> 参与卫戍协议模拟作战的干员将有着"盟约"特性，包括：反映该干员所属阵营的**核心盟约**；反映该干员能力的**附加盟约**。干员名单确定时即固定盟约；一名干员可被分配 1~3 个盟约。
> **每次卫戍协议作战期间，基础载入的盟约有限**，这意味着：
> * **并非所有阵营都能被分配到核心盟约**。
>   * 作为补偿，这类干员可能不会被分配与其阵营相关的盟约，或是以附加盟约中的**协防干员**作为替代。
> * 原型干员会继承其补位干员的盟约。
> * 甄选加入的干员会根据其实际阵营所属分配核心盟约，若没有可匹配的则改为分配**协防干员**盟约。
>
> —— [卫戍协议（总览页）](https://prts.wiki/w/%E5%8D%AB%E6%88%8D%E5%8D%8F%E8%AE%AE)

游戏内过场贴士里还有一条直接描述结果：

> 归属于特定盟约的干员只有部分会出现在调度中心内
>
> —— [卫戍协议：盟约 下半/PRTS盟约记录](https://prts.wiki/w/%E5%8D%AB%E6%88%8D%E5%8D%8F%E8%AE%AE%EF%BC%9A%E7%9B%9F%E7%BA%A6_%E4%B8%8B%E5%8D%8A/PRTS%E7%9B%9F%E7%BA%A6%E8%AE%B0%E5%BD%95) 的「专属贴士」小节

## 二、规律（已能从官方攻略与本期数据对齐的部分）

1. **模式决定可用盟约规模**：[九游玩法攻略](https://www.9game.cn/mrfz/11754940.html) 对【标准模拟】写的是"**无法使用全部盟约**"，【险境模拟】起不再有这条。
   本期数据与之一致：`season.modeDataDict[mode].inactiveBondIdList`
   * `mode_single_funny` / `mode_multi_funny`：13 个可用（8 核心里留 5），缺席 10 个——拉特兰、阿戈尔、卡西米尔、灵巧、奥术、奇迹、投资人、突袭、独行、绝技。
   * `mode_single_normal|hard|abyss` / multi 同档：**23 个全可用，缺席 0 个**。
   * `mode_training_1`（入门协议）：另有 `constData.trBannedBondIds`＝谢拉格、坚守、不屈、迅捷、远见（5 个）。
   * 敌人侧有同构字段 `modeDataDict[].inactiveEnemyKey`（标准/险境 18 条），说明"本模式不登场清单"在这个表里是一等公民。
2. **"被禁用干员"＝所有盟约都不在可用列表里的干员**（与贴士"只有部分会出现在调度中心内"互相印证：只要还有一条可用盟约，该干员就仍在池中）。
   用本期 112 人名册（`charShopChessDatas`）实算：
   * 标准模拟：10 个缺席盟约 → **27 名干员完全不可得**（阿戈尔 7、拉特兰 5、卡西米尔 5、突袭 5、灵巧 4、奥术 4、奇迹 3、独行 3、投资人 1，合计去重 27）。
   * 入门协议：5 个缺席盟约 → **9 名**。
   * 险境/绝境/终极：**0 名**。
   * 只带一条缺席盟约、另一条可用的干员仍会出现在池中（例：`维娜·维多利亚`＝维多利亚＋奇迹 → 标准模拟仍在池；`能天使`＝拉特兰＋奇迹 → 两条都缺席，进禁用名单）。
3. **"不完整"还有第二层含义**：即使盟约可用，调配池也不会包含它的全部成员。原表 `bondInfoDict[].chessIdList` 是**完整成员表**（含助战／自编位），而 `charShopChessDatas` 名册只覆盖其中一部分，例如（本期，按 charId 去重）：
   | 盟约 | 名册成员 | 原表 chessIdList |
   | --- | --- | --- |
   | 助力 | 8 | 13 |
   | 协防干员 | 5 | 8 |
   | 奇迹 | 8 | 11 |
   | 迅捷 | 12 | 14 |
   | 炎 | 10 | 11 |
   | 阿戈尔 | 9 | 9 |
   这就是贴士那句话的字面来源；也解释了为什么策略文案会写「在<某盟约>部分干员缺席时体验可能不完整」（本期 18 条策略带这句，覆盖炎／萨尔贡／维多利亚／谢拉格／拉特兰／阿戈尔／卡西米尔／叙拉古）。
4. **影响面**（原文直接点到的）：
   * 缺席盟约的干员**不进调配池**（商店抽不到）；
   * **自选的支援干员**（物资调配处里的助战／甄选补位，本期上限 20 名）可能因盟约缺席而无法编入；
   * 依赖该盟约的策略／装备／卫戍效果会"体验不完整"，但文案没有说会改写数值。

## 三、没有查到的部分（不要当成已知）

* **数量与随机性**：官方帮助、PRTS 活动页、盟约记录页都没有写"每次载入几个盟约"或"哪些盟约会被随机拿掉"。表里 `bondInfoDict[].weight`（常规 10／投资人·调和·协防·绝技 0）、`maxInactiveBondCount`（全为 -1）、`isActiveInDeck`（全为 false）三个字段语义未确认，**可能是**随机挑选用的权重／上限，但没有证据，不能当规律用。
* **是否每局随机**：文档层面只能确认"模式决定规模"（上表）。玩家侧的第一手描述（[TapTap：谈谈卫戍协议的一些小知识](https://www.taptap.cn/moment/741313468967160412)，2025-11-22，盟约期）写的是"**开局会禁用某几个职业，并且会给出具体的干员**……不要玩到后面才发现自己的干员被禁了"——即每局确实会有"被禁的具体干员名单"，但该帖用的是"职业"这一旧词，且没给数量。这两者之间的差（模式固定清单 vs 每局再随机抽几个）**需要用户按游戏内简报确认**。
* 2024 年一期是**职业制**（商店按职业），当时的"禁用职业"与盟约期的"缺席盟约"不是同一套判定，不能直接照搬。

## 四、本项目实现（2026-09-22 落地）

* 模块：`dist/native-bond-ban.js`（已在 `scripts/build-browser.mjs` 登记）。导出 `banRules(config,data)` / `banPool(data,config)` / `bondBanIds(data,seed,config)`（固定禁用的全部计入，其余从「参与随机」池里抽 3 核心 + 4 附加）、`BOND_BAN_EXCLUDED`（内置固定不被禁的四个盟约）与 `BOND_BAN_DEFAULT_NEVER`（默认方案：那四个 ＋ 投资人）、`BOND_EXEMPT_TABLE`（用户给的 19 条名单）／`resolveBondExempt`、`bondMembers` / `bondRoster` / `charIdOf`、`isOperatorBanned` / `bondBanBlockers` / `bannedOperators`、`bondBanSummary`、`loadBondBan` / `saveBondBan` / `normalizeBondBan` / `defaultBondExempt`。**每个盟约三种状态**（`fixed` 固定禁用／`random` 参与随机／`never` 固定不被禁）存在配置的 `always`／`never` 两个数组里，`banRules` 解析、`banModeOf` 查询；固定禁用不占随机名额。
* 判定：`NativeEconomy.bondBanned(chessId)` 是唯一判定（按 `charId` 归并，精锐形态也命中）。商店／具名池／`later` 池走 `NativeSession.eligible()`；**商店之外的渠道**由 `NativeSession.gain()` 统一返回 `null` 拦下（策略点名发放、卫戍 `SERVER_GAIN_CHAR`、援军转让、道具），晋升奖励候选与 `buy` 再挡一次，`NativeEconomy.draw()` 末尾还有兜底门禁。
* **按盟约随机发人**（策略 `gain_bond_char_per_round`、卫戍 `SERVER_MOST_BOND`、`rewardFromBond`、装备「同盟约干员」、出售计数发放）先问 `bondHasCandidates(id,maxTier)`／`gainableBonds(u,maxTier)`：被禁盟约可能一个人都发不出来，而空候选池会让 `drawFromPool` 抛错、把挂在 prep 上的发放连同「进入下一回合」一起回滚（卡死）。**偏好型**的 `drawFromPool({bond})`（佩佩的特殊刷新、`refreshRequirements`）在没有候选时忽略该盟约偏好、退化成普通抽取。
* 本局禁用在开局定死（`bondBanIds(data,seed,config)`，与商店／波次随机流分开）并随存档保存；旧存档没有 `bondBan` 字段按「本局不额外禁用」补齐；读档时 `sanitizeBannedOffers()` 清掉残留的被禁候选。
* 配置页：「协议自定义」（原「敌人编制台」，用户 2026-09-22 改名）下三个页面，其中两个管盟约禁用——「盟约禁用」页（`native-wave-editor.js` 的 `renderBondBanPage`，动作 `ed-page` / `ed-bb-*`）逐盟约勾选不禁用名单、「禁用方案」页（`renderBondRulePage`，动作 `ed-br-mode` / `ed-br-defaults` / `ed-br-random-all`）逐盟约三选一（固定禁用／参与随机／不被禁）。改动即时写入 `localStorage` 键 `garrison-bond-ban-v1`（**带 `version:2`：临时数据时期没有版本号的旧配置会被忽略并回落到内置名单；同版本但缺 `always`／`never` 的配置按默认方案补齐**），可单独导出／导入 `garrison-bond-ban.json`。`ed-bb-defaults`（「恢复内置名单」）恢复的是 `BOND_EXEMPT_TABLE`，`ed-br-defaults`（「恢复默认方案」）恢复的是「固定禁用为空 ＋ 协防干员／绝技／调和／独行／投资人不被随机禁」。
* 简报：战前准备页新增「本局缺席盟约」卡组与「本局拿不到的干员」清单（`native-play.js` 的 `bondBanBriefing`），与对局实际使用的记录是同一份。
* 回归：`tests/native-bond-ban.test.mjs`（判定、抽取、charId 归并、非商店渠道、读档、配置读写、编制台页面）、`tests/native-bond-ban-ui.test.mjs`（接线门禁）。单测需要完整名册时显式传 `bondBan:NO_BOND_BAN`（`tests/no-bond-ban.mjs`），`tests/effects-harness.mjs` 的 `openBattle` 已默认关掉。

## 五、边界问题（全部已拍板）

1. **0 成员／低成员盟约是否参与随机抽取？** **已拍板：协防干员／绝技／调和／独行固定不被禁，不进抽取池**（用户 2026-09-22）；同日追加：**默认方案里投资人（`investShip`，4 人，`weight=0`）也不被随机禁用**（`BOND_BAN_DEFAULT_NEVER`），所以默认池是 8 核心 + 10 附加共 18 个。三种状态在「协议自定义 → 禁用方案」页可改，`banPool(data,config)` 是唯一入口。
2. **效果点名发放的干员要不要也拦？** **用户 2026-09-22 追加口径：要拦**——策略／道具／卫戍等非商店渠道同样受禁用限制，实现见第四节。
3. **被禁盟约能不能被装备"借"到？** **用户 2026-09-22 口径：道具发放的盟约不受影响**。当前实现：装备自身的 `giveBondId` 归属照旧用于商店／具名池取货，变形同构体照旧把另一件装备的盟约借给携带者；受限的只有干员的获取。
4. **沙盒（技能测试）是否禁用盟约？** 测试工具需要全库。**当前实现：不禁用**（`native-play.newSandbox` 显式传 `bondBan:{bonds:[],exempt:{}}`）。
5. 标准模拟官方攻略写着"无法使用全部盟约"，但用户口径要求不看模式表——实现上以用户口径为准（每局随机 3+4 对所有难度一致）。
6. **「不禁用名单」的语义（用户 2026-09-22 确认＝A，已实现）**：一名干员常同时属于 2–3 个盟约，两种读法差别很大。用户原话「只要不在某个盟约的名单就被禁，即不被所有禁用名单禁用才保留」：
   * **A（当前实现，逐盟约）**：只要挂在某个被禁盟约名下、又不在**那份**名单上 → 禁用；**只有被所有（属于它的）被禁盟约的名单都写上才保留**。名单条目会被别的被禁盟约抵消——例：`空弦` 在拉特兰名单上，但灵巧被禁时仍然出局；`至简` 在萨尔贡名单上，灵巧被禁时萨尔贡变成 0/10 幸存（seed 3）；反过来 `烛煌` 在维多利亚与炎两份名单上，这两个盟约同时被禁也保留。
   * B（并集，**已否决**）：只要出现在任一被禁盟约的名单上就放行。
   实测（2000 局 3+4）：**A 出局中位 48 人 / B 中位 44 人**。回归：`tests/native-bond-ban.test.mjs` 的「名单语义（用户 2026-09-22 确认）」。

## 六、用户提供的名单 → 本项目解析

用户 2026-09-22 给出的原始名单（游戏内显示名＋阶）：

| 盟约 | 原始名单 | 盟约 | 原始名单 |
| --- | --- | --- | --- |
| 阿戈尔 | 斯卡蒂III | 坚守 | 斯卡蒂III、信仰搅拌机IV、余VI |
| 卡西米尔 | 砾II、锏VI | 精准 | 送葬人II、雪猎III、远牙IV、缇缇V、异客VI |
| 拉特兰 | 空弦III、圣约送葬人V | 灵巧 | 灵知IV |
| 萨尔贡 | 至简III | 奇迹 | 伺夜III、维娜维多利亚VI |
| 维多利亚 | 哈洛德II、烛煌V | 投资人 | 凛御银灰V |
| 谢拉格 | 哈洛德II、锏VI | 突袭 | 斯卡蒂III、瑕光III |
| 叙拉古 | （无） | 迅捷 | 凛御银灰V、异客VI、锏VI |
| 炎 | 烛煌V | 远见 | 初雪III、风笛IV |
| 奥术 | 洛洛II、莫斯提马IV、圣聆初雪VI | 助力 | 耶拉III |
| 不屈 | 风笛IV | | |

解析结果（`resolveBondExempt`，零条未解析）：19 个盟约全部命中，「名＋阶」正好等于名册里的 charId（阶用来区分同名干员：初雪III↔圣聆初雪VI、银灰IV↔凛御银灰V、斯卡蒂III↔浊心斯卡蒂VI、送葬人II↔圣约送葬人V、维娜·维多利亚VI 的间隔号被忽略）。名单规模与被禁时的杀伤：

| 盟约 | 名单/成员 | 被禁出局 | 盟约 | 名单/成员 | 被禁出局 |
| --- | --- | --- | --- | --- | --- |
| 炎 | 1/10 | 9 | 精准 | 5/12 | 7 |
| 萨尔贡 | 1/10 | 9 | 迅捷 | 3/12 | 9 |
| 维多利亚 | 2/10 | 8 | 灵巧 | 1/11 | 10 |
| 谢拉格 | 2/10 | 8 | 奥术 | 3/9 | 6 |
| 拉特兰 | 2/10 | 8 | 坚守 | 3/10 | 7 |
| 阿戈尔 | 1/9 | 8 | 助力 | 1/8 | 7 |
| 叙拉古 | 0/10 | 10 | 远见 | 2/8 | 6 |
| 卡西米尔 | 2/10 | 8 | 奇迹 | 2/8 | 6 |
| 投资人 | 1/4 | 3 | 突袭 | 2/9 | 7 |
| 不屈 | 1/7 | 6 | | | |

3 核心 + 4 附加的抽样强度（2000 个 seed）：**出局 41～56 人，中位数 48 / 112**（此前临时数据下约 19，真实名单的杀伤明显更大，属于预期）。理论上限是 19 个可禁盟约全被抽中，但每局只抽 7 个；把 19 个全禁掉（对照用）是 105 / 112。

维护方式：改 `BOND_EXEMPT_TABLE`（源码即事实），或在「协议自定义 → 盟约禁用」页勾选名单、「禁用方案」页改三种状态（`localStorage` 带 `version:2`，旧配置会被忽略；同版本缺 `always`／`never` 的按默认方案补齐）。

### 名单条目的实际效力（按已确认的语义）

因为「只有被**所有**属于它的被禁盟约的名单都写上才保留」，一名干员只在 1 个盟约的名单上、而它还有别的盟约时，只要那个盟约被禁，这条名单就同时失效。26 名上榜干员里只有 4 名是全写了（**哈洛德**：维多利亚／谢拉格；**锏**：谢拉格／卡西米尔／迅捷；**斯卡蒂**：阿戈尔／坚守／突袭；**烛煌**：炎／维多利亚）——其余 22 名都有一条「漏写的盟约」：

| 干员 | 已写进 | 还漏写的盟约（被禁时该条名单失效） | 干员 | 已写进 | 还漏写的盟约 |
| --- | --- | --- | --- | --- | --- |
| 初雪 | 远见 | 谢拉格 | 缇缇 | 精准 | 萨尔贡 |
| 伺夜 | 奇迹 | 叙拉古 | 维娜·维多利亚 | 奇迹 | 维多利亚 |
| 风笛 | 远见／不屈 | 维多利亚 | 瑕光 | 突袭 | 卡西米尔 |
| 空弦 | 拉特兰 | 灵巧 | 信仰搅拌机 | 坚守 | 拉特兰 |
| 砾 | 卡西米尔 | 不屈 | 雪猎 | 精准 | 谢拉格 |
| 凛御银灰 | 迅捷／投资人 | 谢拉格 | 耶拉 | 助力 | 谢拉格 |
| 灵知 | 灵巧 | 谢拉格 | 异客 | 精准／迅捷 | 萨尔贡 |
| 洛洛 | 奥术 | 维多利亚 | 余 | 坚守 | 炎 |
| 莫斯提马 | 奥术 | 拉特兰 | 远牙 | 精准 | 卡西米尔 |
| 圣聆初雪 | 奥术 | 谢拉格 | 至简 | 萨尔贡 | 灵巧 |
| 圣约送葬人 | 拉特兰 | 远见 | | | |

如果希望某名干员「无论哪几个盟约被禁都能用」，就得把它补进上表「还漏写」的那几个盟约的名单里；不做补充就保持当前行为（语义 A）。

来源汇总：[卫戍协议（总览）](https://prts.wiki/w/%E5%8D%AB%E6%88%8D%E5%8D%8F%E8%AE%AE)、[卫戍协议/帮助](https://prts.wiki/w/%E5%8D%AB%E6%88%8D%E5%8D%8F%E8%AE%AE/%E5%B8%AE%E5%8A%A9)、[卫戍协议：盟约 下半](https://prts.wiki/w/%E5%8D%AB%E6%88%8D%E5%8D%8F%E8%AE%AE%EF%BC%9A%E7%9B%9F%E7%BA%A6_%E4%B8%8B%E5%8D%8A)、[盟约记录（含专属贴士）](https://prts.wiki/w/%E5%8D%AB%E6%88%8D%E5%8D%8F%E8%AE%AE%EF%BC%9A%E7%9B%9F%E7%BA%A6_%E4%B8%8B%E5%8D%8A/PRTS%E7%9B%9F%E7%BA%A6%E8%AE%B0%E5%BD%95)、[九游玩法攻略](https://www.9game.cn/mrfz/11754940.html)、[TapTap 玩家小知识](https://www.taptap.cn/moment/741313468967160412)。
