# 战斗系统规范

更新：2026-09-08。运行规则版本：`battle.rulesVersion = 2`。

本文件是后续战斗开发的接口与验收基线。基础数值运算按公开的原作机制建立；现有干员、盟约、地图、技能数值仍属于同人改编数据。不得将本次基础层接入解释为已经完成原作全部机制或逐帧复刻。

## 1. 分层及职责

| 层 | 文件 | 输入 → 输出 | 不负责 |
| --- | --- | --- | --- |
| 数据 | `dist/data.js` | 单位属性、格子、技能、攻击参数 | 推进时间 |
| 数值 | `dist/combat.js` | 属性修饰器、伤害参数 → 数值／HP变化 | 搜索目标、播放动画 |
| 范围与索敌 | `dist/targeting.js` | 空间、候选对象、优先级 → 有序目标 | 造成伤害 |
| 攻击动作 | `dist/actions.js` | 目标ID、攻击数据、帧数 → 出手事件 | 自己查询世界、直接扣血 |
| 对局协调 | `dist/engine.js` | 固定帧 → 行动、弹道、结算、事件 | 从画面帧率推算命中 |
| 显示 | `dist/renderer.js` | 对局状态 → Canvas画面 | 改变战斗结果 |

运营资金、战斗部署费用 DP、干员技力 SP、防卫目标生命、单位 HP 是不同资源。费用、自动再部署和关卡超时恶化属于卫戍协议改编模式规则，不属于通用伤害公式。

## 2. 属性和伤害公式

依据：[PRTS 游戏数据基础：属性、伤害、攻速](https://prts.wiki/w/游戏数据基础)。核对日期为本文件更新日；该站是玩家维护的机制研究资料，不是官方引擎接口。

下面比例均采用小数：30% 写作 0.3。内部保留小数，只有画面数字取整。

```text
属性 V = clamp(((基础 B + 直接加算 F) × max(0, 1 + 同类比例加成之和 P)
               + 最终加算 I) × 最终倍率之积 M, 属性下限, 属性上限)

攻击基数 A = max(0, 最终攻击力 × 攻击倍率 + 附加攻击力)
有效防御 De = max(0, 目标最终 DEF - 固定物穿) × (1 - 百分比物穿)
有效法抗 Re = max(0, 目标最终 RES - 固定法穿) × (1 - 百分比法穿)
物伤 Dp = max(0.05 × A, A - De)
法伤 Da = max(0.05 × A, A × max(0, 1 - Re / 100))
真伤 Dt = A
基础治疗 H = A
最终伤害 D = 对应基础伤害 × 最终伤害倍率 × (1 - 减伤比例)
```

属性修饰器的 `scales` 支持最终倍率；负值按 `1 + 值` 解释，例如 -0.4 代表剩余60%。同类比例加成加算，最终倍率乘算。降低目标防御属于目标属性修改；穿透只参与本次攻击。

例如攻击1000、防御300：攻击倍率2得到1700伤害，最终伤害倍率2得到1400伤害。攻击300、目标防御200的三段攻击结算为100×3，不能先合并900攻击再扣一次防御。

普通技能“攻击力提高”进入属性比例层；“攻击力的若干倍”进入攻击倍率层；易伤、模式增伤进入伤害层。同名干员精锐倍率1.8与盟约系数来自现有改编内容。

## 3. HP、护盾、治疗

```text
吸收值 = min(当前数值护盾, D)
实际 HP 损失 = min(max(0, 当前HP - 不死下限), max(0, D - 吸收值))
新 HP = 当前HP - 实际HP损失
实际治疗 = min(最大HP - 当前HP, max(0, H × 治疗倍率))
持续再生 = min(缺失HP, 最大HP × 每秒再生比例 × dt)
```

`applyDamage` 分别返回 HP 损失、护盾消耗和二者之和。战斗统计不计算溢出伤害。HP为0的目标不能用普通治疗复活。不死效果将最低HP保留为1；无敌直接拒绝伤害。物理／法术闪避在数值伤害前判定，真伤跳过这两类闪避。

当前 `shield` 仅表示数值护盾。新增 `barriers` 为独立次数屏障，可用 `types` 指定物理／法术等允许拦截的伤害类型；无敌、闪避和 HP 变化仍使用独立语义。未为每种原作盾完成逐技能接入。

当前运行中最大HP发生变化时按原HP比例调整；这是本项目采用的变更策略，原作个别技能对最大HP和当前HP有独立处理，导入时需要单独匹配。持续再生不经过普通治疗倍率，也不记入普通治疗统计。

## 4. 技力与技能

技能定义与基础分类参考：[PRTS 技能](https://prts.wiki/w/技能)。当前适配器提供以下事件接口：

| `recovery`／事件 | 实际触发 | 回复量 |
| --- | --- | --- |
| `auto` | 存活且部署后的模拟时间 | 自然回复速度 × dt |
| `attack` | 普攻出手时 | 默认1；可由攻击配置覆写 |
| `defensive` | 成功接受攻击伤害，包括数值护盾吸收 | 默认1；攻击数据可禁止 |
| `external` | 外部效果显式调用 | 效果指定值 |

```text
SP上限 = 技能消耗 × 可存储次数（默认1）
SP新值 = min(SP上限, SP旧值 + 合法回复量)
释放后SP = SP旧值 - 本次消耗
```

自然回复加速不自动放大攻击／受击回复。技能持续期、`spLock`期间、未部署或倒地时停止上述回复。扣除一次消耗，保留合法充能余量，不统一清零。

`activation` 分为 `auto`、`manual`、`nextAttack`。手动入口为 `Game.castSkill(uid)`；当前界面仍以改编内容的自动施放为主，没有新增手动按钮。下次攻击类在开始下一次攻击时消费技力，替换该次攻击；不额外附送一次普攻，也不会用该强化攻击给自身补回1技力。

当前克洛丝已经接入攻击回复／下次攻击；其他旧技能默认维持 `auto`，不代表它们的原版技能都是自动回复。导入原作数据时必须逐技能核定初始SP、消耗、恢复方式、触发方式、持续时间和锁定时间。分段攻击回复次数与剑豪等例外需要使用明确的事件参数，不能依据命中敌人数推断。

## 5. 攻击范围与索敌

空间与动作概念参考：[PRTS 作战机制](https://prts.wiki/w/游戏机制进阶)。

- `rangeCells` 是以单位中心为原点的局部格子数组，+x向前；部署朝向把它旋转为世界格子。显示高亮和普通攻击碰撞共享该数组。
- `containsTarget` 只回答是否相交：格子半宽0.5，默认以目标中心检测，可显式提供 `hitRadius`。这与动画／头像大小无关。
- 范围决定候选目标，`antiAir` 决定能否攻击飞行单位，`targetPriority` 决定顺序；三者独立。
- 干员当前通用顺序：自身阻挡目标 → 特定分支对空优先 → 嘲讽 → 剩余路线距离最短 → 稳定UID。
- 敌方通用顺序：阻挡者 → 射程内嘲讽 → 最近部署时间 → 稳定UID。再部署刷新部署时间。
- 医疗只选存活、已部署、可治疗且受伤的友方，按HP比例由低到高排列。
- 不可选中目标被排除；隐匿敌人在被阻挡后可进入普通索敌。
- 溅射先选择主目标，命中后再按落点和溅射半径寻找受害者。溅射半径不是索敌距离。

当前地图路径由相邻地块组成，剩余距离按“当前位置至下一路径点 + 后续线段长度”计算，已去掉旧版只比较剩余节点数量的误差。瞬移、等待点、特殊路径和原版完整仇恨过滤器尚未导入。

原作存在显示视野、技能选择范围、攻击触发范围不一致的特殊配置。当前普通攻击统一范围；特殊技能必须显式声明独立范围，不能用修改高亮格子的方式假装完成技能索敌。现有范围形状仍是改编配置，尚未逐人替换成原始 `rangeId` 数据。

## 6. 攻击动作、弹道与时间

理论间隔 `T = T0 / (clamp(ASPD, 10, 600) / 100)`；属性阶段攻速下限20，默认100。当前运行采用30Hz固定步长，将间隔和前摇四舍五入为整数帧并保证至少1帧。前摇与攻击间隔分别存储。

```text
索敌成功
  → 锁定目标ID，开始前摇，设置攻击周期倒计时
  → 前摇结束，产生出手事件
      → 无弹道：立即进入命中结算
      → 有弹道：创建独立追踪弹道，抵达后结算
  → 攻击者等待剩余周期（后摇／恢复）
  → 下一次索敌和攻击
```

攻击者结束前摇不等于目标立即掉血。后摇等待与弹道飞行可同时发生。目标死亡或离场时，普通追踪弹道失效，不自动改追另一目标。已发射弹道可在攻击者倒地后继续；尚未出手的动作在倒地或眩晕时取消。远程敌人攻击时停步。

`action`保存前摇剩余帧与攻击载荷，`attackCooldown`保存周期余量，`projectiles`保存独立位置和目标ID。旧版 `cd`仅用于旧存档迁移，不是新的出手依据。

## 7. 已知精度边界与下一步

以下内容尚不能宣称与原作逐项一致：

1. 35名干员原始范围、前摇帧、抬手动画、弹速及技能动作表。当前前摇默认基础间隔的30%，远程弹速6格/秒，是显式适配参数。
2. 原客户端定点数误差、动画事件量化、3帧索敌节奏与特殊攻击重置规则。当前每个模拟帧查询候选目标。
3. 普攻攻击力在动作开始时保存；目标防御和模式易伤在命中时计算。原作不同攻击／技能的属性取样时点需要逐项校准。
4. 旧版瞬发群体技能仍通过技能适配器处理；特殊技能弹道、逐段时间偏移、元素损伤累积／爆发、位移物理和复活机制未实现。通用状态抵抗及独立元素伤害类型已有原语，不等于完成全部原作应用。
5. 手动技能界面、每名单位的原版恢复方式和原版养成属性尚未导入。
6. 当前 legacy 内容版本 5，内容版本不匹配的旧存档保留备份，不静默改写为新规则。

接续顺序：锁定原作数据版本 → 导入并核对单位／技能配置 → 特殊机制扩展 → 原版对照场景验收。每次新增机制都应提供独立规则测试和至少一个通过实际 `Game` 的集成用例。

## 8. S04 本轮补充

`status.js` 把控制权限、免疫／抵抗、持续时间和属性变化分开。冻结降低法术抗性；睡眠目标的索敌许可与伤害许可独立；沉默仅限制显式标记可沉默的能力。实际效果执行仍须逐技能定义。

`protocol.js` 按下半期历史表解析固定养成、默认技能／模组与自动操作策略。自动操作遵守 3 秒冷却；通用技能定义并不因此变成原作全量可执行技能。具体剩余项见 S4_S6_PROGRESS.md。

## 9. 验证

运行 `npm run build` 更新离线脚本，运行 `npm test` 验证全部测试。

`tests/combat.test.mjs`新增12项规则／集成测试：属性层、三类伤害、穿透顺序、分段伤害、HP和护盾、SP分类及锁定、攻速和前摇、范围、索敌、弹道、存档、固定步长。这一历史基础批次为23项；当前包含新增数据、会话、部署、状态、原作运营及木桩测试，共75项。真实浏览器画面验收不包含在Node测试中。
