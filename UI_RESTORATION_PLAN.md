# 卫戍协议：盟约下半期——界面与操作还原计划

更新：2026-09-12。对应整体计划 S0–S10；本文件定义实施与验收任务，主要界面与操作原型已实施，软件测试通过；原作完整对照仍未关闭，详见S0_S3_DELIVERY.md。

## 1. 目标和参考范围

界面结构、视觉层级、控件位置、信息呈现、操作顺序、确认／取消行为、阶段可用操作与反馈一起还原。目标为《卫戍协议：盟约》下半期完整开放后的国服环境。

以该模式PC端键鼠操作为桌面基线，以同一版本移动端横屏操作为触控基线。两者分别记录差异，不能把普通明日方舟关卡操作直接套用到卫戍协议。当前网页保留作为迁移时可运行的基线。

已核实的交互约束来自[官方下半期更新公告](https://ak.hypergryph.com/news/5114)：整备区可配置装备、满手牌时仍可交换整备区与战场干员、单人暂离和恢复，以及PC端专属按键适配。准确键位、手势、按钮位置和动效需进一步从同版本原作取证。

当前没有完成原作全套界面截图和操作录像归档。因此不把现有橙色终端配色、三栏网页布局、弹窗组织或快捷键认定为原作标准，不凭印象编造像素尺寸。

## 2. 页面与状态清单

下表是需要覆盖的功能页面／状态，不保证每项都对应原作独立页面；S0根据实际导航确定其属于全屏、侧栏、浮层还是弹窗，并记录原作名称。

| ID | 功能范围 | 必须覆盖的状态 |
| --- | --- | --- |
| UI-01 | 模式大厅与主导航 | 首次进入、已有存档、内容锁定、暂离后返回 |
| UI-02 | 物资调配与干员配置 | 等阶分组、预设／甄选／助战、技能／模组选择、筛选、详情和不可选原因 |
| UI-03 | 独立／同盟与难度选择 | 未解锁、已解锁、说明、返回；房间相关入口 |
| UI-04 | 战前简报与初始策略 | 敌方信息、禁用／缺席信息、候选策略、查看详情、选择和确认 |
| UI-05 | 阵地休整 | 初始资源、招募、升级、刷新、冻结、手牌和溢出区、布阵、装备、盟约 |
| UI-06 | 战场中的选中与操作浮层 | 范围、朝向预览、属性来源、装备、移动／交换、撤回／出售、取消 |
| UI-07 | 自动战斗 | 敌人波次、DP、HP／SP、技能状态、阻挡、倒地／再部署、阶段进度及原作允许的控制 |
| UI-08 | 回合结束与机变决策 | 结果反馈、候选选择、队伍选择顺序、计时与强制阶段转换 |
| UI-09 | 同盟房间、队伍概览与联防 | 成员、准备、剩余决策时间、切换战场、等待、联防、观战、断线重连 |
| UI-10 | 暂离／放弃／失败／成功结算 | 退出前信息、保存状态、恢复有效期、认证／奖杯／解锁及再次进入 |
| UI-11 | 模式资料与设置 | 盟约／策略／装备说明、敌人信息、操作帮助、音量与显示；按原作已有入口归位 |
| UI-X | 本软件扩展功能 | 数据浏览、内容包、创作工具、调试／回放；入口与原版游玩流程明确区分 |

每个适用控件至少覆盖：正常、悬停、焦点、按下、选中、禁用及原因、等待、失败。每个数据页面覆盖加载、空内容、加载失败、版本不兼容和恢复。

## 3. 取证和设计交付

### S0：建立可复核的参考包

每份参考记录活动修订版、平台、分辨率、UI缩放、阶段、前置状态、来源链接及视频时间码。截图取自实际原作；录制或公开资料无法确认的内容标记待核实。

对重要交互记录“操作前—输入—中间状态—结果—取消分支”，静态截图不能证明完整操作逻辑。PC专属按键不根据当前代码中的R/E/Q/Delete推测。

生成页面索引、导航关系、控件状态清单、操作矩阵和素材缺口表。对跨期截图进行版本筛选。

### S3：还原规范与可点击原型

- 从原作提取布局比例、锚点、安全区、信息密度、卡片排列、商店／手牌组织和浮层层级。
- 定义颜色、字号、字重、数字样式、间距、描边、圆角、阴影和动效令牌，数值来自参考测量。
- 字体优先匹配可用原作资源；资源缺失时记录回退方案及字宽差异，不能把默认字体当作完成。
- 战场单独记录视角、地块投影、部署标记、范围边界、单位比例、状态条、特效和遮挡关系。
- 可点击原型覆盖 UI-01 至 UI-08 的主要单人操作链；UI-09先覆盖房间与成员状态。
- 原型即使使用测试状态，也必须明确标记，不把假的资金变化或伤害统计作为真实运行验收。

参考截图画幅作为视觉比对基准；额外验证常见桌面尺寸、宽屏、浏览器缩放和移动端横屏。窄屏仍必须可用，但不把人为纵向重排伪称为原版横屏布局。

## 4. 操作契约

所有操作都记录：可执行阶段、选中对象、输入动作、预览、提交条件、产生的命令、成功反馈、拒绝原因、取消后的状态、原作证据ID。

| 操作 | 需要还原和验证的内容 |
| --- | --- |
| 招募／购买 | 点击目标、费用显示、资金和槽位检查、扣款时点、入手位置、合成触发和重复输入 |
| 刷新／冻结／升级 | 费用变化、免费次数、剩余货物、锁定状态、等级变化、快捷键及操作反馈 |
| 手牌选择 | 点击与拖动如何区分；选中态、详情、点击空白／另一个对象后的行为 |
| 新部署 | 用户已明确：卡片拖到合法地块 → 进入朝向选择 → 拖动朝向 → 松手确认；确认前不修改位置。已实现并通过键鼠／真实触控验证 |
| 朝向 | 四向预览，中心松手／暗处点击／Esc／触摸取消均撤销；位置和方向原子提交。另提供方向按钮＋确认和键盘操作 |
| 场内移动／交换 | 起点终点、双方合法性、方向保留、部署位与手牌满的处理、召唤物联动 |
| 手牌与场内交换 | 特别覆盖下半期的满手牌交换规则，不沿用旧版禁止交换的条件 |
| 撤回／出售 | 入口、目标、收益与物品返还、确认方式及取消；不额外添加未经确认的操作步骤 |
| 装备 | 场上和整备区目标、已有装备展示、两件上限、替换选择、取消、合成及返还 |
| 法术／召唤物 | 选择对象或地块、范围、数量、使用限制、预览取消和归属生命周期 |
| 干员／盟约／敌人详情 | 进入与返回、是否保留选中、层数与来源、基础属性与局内实际属性区分 |
| 初始策略／机变 | 候选数量和布局、说明、选中／确认、是否允许反悔、单人和同盟时间规则 |
| 开始／结束回合 | 准备状态、预算／溢出提示、阶段切换锁定；确认弹窗按原作取证确定 |
| 暂停／倍速／战斗控制 | 先核实该模式实际提供的操作；现有1/2/4倍速和空格暂停不能自动视为原版功能 |
| 暂离／恢复 | 写入时点、保存提示、有效期、本地与服务端时间、放弃结算和再次进入 |
| 队友战场／观战 | 查看权与控制权、切换反馈、退出后状态、断线／重连状态与阶段计时 |

引擎支持手动技能是通用能力，卫戍协议界面是否允许手动释放由该期规则决定。不能因为存在 `Game.castSkill` 就给所有自动战斗单位加释放按钮。

## 5. 输入和状态架构

把三个层面独立保存：

- 对局状态：休整、作战、联防、机变、结算等。
- 操作状态：无选中、查看、持有卡片、部署预览、方向预览、装备目标、替换选择、等待提交等。
- 展示状态：当前页面、浮层、焦点、滚动和布局。

具体状态按原作所需分支建立；不能把所有操作简化为一个selected字段，也不能让“打开任意弹窗”自动等于“暂停模拟”。

键鼠与触控共享同一命令入口；一次有效提交只产生一条命令。界面预览不扣钱、不移动实体、不消耗物品。取消、指针丢失、切换页面或阶段发生变化时清理预览，并按实际权威状态恢复。

屏幕坐标到地块坐标的转换由战场视图提供，缩放、窗口变化、触控位置与可见高亮必须一致。范围预览消费引擎范围查询结果，独立的显示视野仍按原作配置处理。

键位从实际PC端取证；按键重复、输入框、对话框、浏览器快捷键及焦点恢复单独验证。触控验证拖动取消、指针捕获、屏幕边缘、缩放和小目标误触。

本地页面失焦与同盟模拟全局暂停是不同事件。同盟阶段时间由服务器决定，打开详情或切换标签不能停止其他玩家的对局。

## 6. 对现有文件的实施分工

| 现有文件／接口 | 计划改动 |
| --- | --- |
| `dist/app.js` | 拆分页面呈现、交互状态和输入转命令；迁移集中在一个dialog里的页面；移除由弹窗／页面可见性直接决定所有模式时间的耦合 |
| `dist/style.css` | 用原作参考令牌重建布局和控件；处理缩放、选中、禁用、焦点和触控状态 |
| `dist/renderer.js` | 校准投影、坐标拾取、范围、单位尺寸、状态条、动作和特效；模拟时钟与视觉时钟分开 |
| `dist/engine.js` | 提供命令校验、允许动作、预览查询和模式阶段政策；修复UI无法绕过的非法操作路径 |
| 数据与模式内容包 | 为界面提供真实名称、图标、等阶、说明、状态与可用配置；不在HTML中另写一套玩法数据 |
| `scripts/build-browser.mjs` | 随源模块拆分更新打包清单，保留离线启动；不只修改生成的bundle |
| 现有测试 | 保留计算和启动回归；补真实浏览器交互、视觉基准和多客户端测试 |

是否引入框架、动画运行库或其他渲染方案，在S2以实际资源和需求验证后决定；本计划不预设必须重写为React、Three.js或某个游戏引擎。

## 7. 验收方法

### 视觉

同分辨率、同状态、同语言下并排和叠加对比原作截图。分别检查页面构图、关键控件位置、卡片尺寸、字体数字、战场视角和浮层层级。差异按区域登记，先解决会改变信息读取或操作的偏差。具体几何误差阈值在参考截图确定后冻结；动态特效和字体渲染误差使用专门规则，不能用一个整体相似度掩盖按钮错位。

### 操作

同一任务逐步记录点击／拖动／键位、画面状态、命令次数和最终对局状态。原作不需要的中间步骤不能为实现方便强行增加。取消任何未提交操作，都不能留下资源或单位状态变化。

至少覆盖：购买并部署；满手牌交换；三合一与装备返还；装备合成与替换取消；刷新冻结升级；非法地块；部署操作中阶段切换；查看详情和返回；单人暂离恢复；同盟决策超时及重连。

### 工程

真实浏览器验证键鼠和触控；Node VM启动测试不能代替这一步。检查加载／空／错误状态、长名称、内容包缺资源、窗口缩放、中文输入、焦点和文字对比度。

回放比较“不同输入方式最终产生相同合法命令序列”；服务器拒绝后界面必须回到真实状态。视觉帧率下降不能漏掉模拟事件或多发命令。

## 8. 交付顺序

S0归档参考 → S3可点击原型 → S4战场表现与战斗联调 → S5真实单人操作闭环 → S6全内容适配 → S7同盟界面 → S9细节精修 → S10正式验收。

每个阶段同时交付可查看成果、参考证据、差异列表和测试结果。界面布局未核实、关键操作未对照或仍使用临时素材时，分别标记未完成，不能仅凭“风格接近明日方舟”宣布通过。
