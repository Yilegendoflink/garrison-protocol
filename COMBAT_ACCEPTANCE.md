# 公共战斗与基础反馈修复验收

日期：2026-09-14。对象：当前工作区源码及重新生成的浏览器 bundle。

结论：本轮列出的阻塞缺陷已修复并通过复验。未提交或发布远端；用户原有修改保留。

## 已修复

- 出生位置：路线保留起点，不再从第一目标点出生。
- 事件：事件类型与伤害类型分离，命中、攻击、范围反馈能被消费者识别；事件编号避免同一模拟时刻丢音效。
- 远程敌人：有攻击目标时停步，消失状态不能攻击，消失时取消未释放动作。
- 路线时间：等待和移动共用剩余时间预算；传送不增加行走距离；等待／消失的占位坐标不产生额外路径。
- 延迟攻击：多段排队，余震延迟结算，尚未释放的攻击检查施法者状态；已发射弹体保留独立生命周期。
- 技能与属性：支持显式攻击重置配置，移除“立即”文案推断；已实现的模式倍率与技能比例分层并显示来源。
- 表现：补充部署／技能结束标记、攻击方向反馈、范围投影修正、音量控制与音频节点回收。
- 兼容：旧战斗路线恢复、受限存储环境、休整期部署范围预览。

## 验证证据

1. 66 项针对性 Node 检查通过，0 失败。覆盖公共战斗、主战斗集成、技力、运营、协议与波次。主战斗集成检查实际出生点、远程停步、命中事件、消失停止攻击及倍率组合。
2. 独立 Edge 浏览器 8 项检查通过，0 页面异常：离线启动、购买部署、作战布局、暂停画布完全冻结、音量持久化、静音／减少动效、4 倍速选择、恢复运行。
3. `node scripts/build-browser.mjs` 完成；其内置语法解析检查通过。
4. 浏览器截图经人工视觉检查，战场、状态、时间和控制项无相互遮挡（1440×1000）。截图与机器结果位于 `artifacts/combat-acceptance/`。

复验命令：

```powershell
node --test tests/combat.test.mjs tests/native-protocol.test.mjs tests/native-economy.test.mjs tests/native-wave-random.test.mjs tests/native-combat.test.mjs tests/native-sp.test.mjs
node scripts/build-browser.mjs
node scripts/regression-browser.mjs combat      # 原 scripts/combat-acceptance.cjs
```

## 适用边界

本结论是公共战斗及基础反馈的工程复验，不是全原作逐帧一致性证明。前摇默认 30%、弹速 6 格/秒、多段及余震默认间隔 2 帧仍为已标注的适配参数；逐技能重置配置尚未全量取证。复杂召唤、形态、全敌人特殊能力和完整角色美术仍不属于本轮修复范围。浏览器声音路径已验证，音色听感和移动端全尺寸不在本轮结果内。
