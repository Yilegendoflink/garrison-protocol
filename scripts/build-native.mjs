import fs from 'node:fs/promises';
import {resolveChess,applyEnemyOverrides} from '../dist/protocol.js';
const source=JSON.parse(await fs.readFile('data/modes/alliance-lower/source.json','utf8')),base=JSON.parse(await fs.readFile('data/normalized/allianceLower.json','utf8')),catalog=JSON.parse(await fs.readFile('data/modes/alliance-lower/catalog.json','utf8')),manifest=JSON.parse(await fs.readFile('data/modes/alliance-lower/levels/manifest.json','utf8'));
const profiles={};for(const shop of Object.values(source.season.charShopChessDatas)){if(!shop.charId)continue;const entity=base.entities[shop.tmplId||shop.charId];for(const id of [shop.chessId,shop.goldenChessId]){if(!id)continue;const states=[];for(let i=0;i<entity.skillRefs.length;i++)states.push(resolveChess(source,base,id,{skillIndex:i}));const selected=resolveChess(source,base,id);profiles[id]={...selected,profession:entity.profession,branch:entity.branch,position:entity.position,skillChoices:states,asset:catalog.roster.find(r=>r.charId===shop.charId)?.asset};}}
const levels={};for(const[id,entry]of Object.entries(manifest.files)){const l=JSON.parse(await fs.readFile('data/modes/alliance-lower/levels/'+entry.file,'utf8'));const enemyProfiles={};for(const ref of l.enemyDbRefs||[]){const raw=base.enemies[ref.id]?.levels.find(x=>x.level===ref.level);if(raw)enemyProfiles[ref.id]=applyEnemyOverrides(raw.data,ref.overwrittenData);}levels[id]={routes:l.routes,waves:l.waves,enemyProfiles};}
const assets=JSON.parse(await fs.readFile('dist/assets/prts/manifest.json','utf8'));const data={...source,profiles,ranges:base.ranges,maps:catalog.maps,enemies:Object.fromEntries(catalog.enemies.map(e=>[e.id,e.data])),items:catalog.items,assets:Object.fromEntries(Object.entries(assets.assets).map(([id,a])=>[id,a.file])),levels,limitations:['服务端商店等阶权重尚未取得，手动验收版采用当前可选池等权抽取。','技能通用属性、技力与弹药已接入；特殊召唤、形态、动画释放帧和部分敌人能力仍存在差异。','部分复杂策略、地图环境和装备联动尚未完整实现，请通过反馈入口记录。'],version:'manual-2026-09-13'};
await fs.writeFile('dist/runtime-data.js','// Generated historical mode runtime data.\nexport const NATIVE_DATA = '+JSON.stringify(data)+';\n');console.log('Native runtime: '+Object.keys(profiles).length+' cultivation states, '+Object.keys(levels).length+' levels');
