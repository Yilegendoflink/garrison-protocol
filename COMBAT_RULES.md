# 公共战斗规则核对表

本表对应 native 客户端当前公共战斗链路（`dist/native-combat.js`、`dist/native-battle.js`、`dist/native-sp.js`、`dist/status.js`）。只记录已校准的公共过程；全干员／全敌人专属机制不在本阶段完成范围内。

数值来源以 [PRTS 游戏数据基础](https://prts.wiki/w/游戏数据基础)、[PRTS 作战机制](https://prts.wiki/w/游戏机制进阶)、[PRTS 技能](https://prts.wiki/w/技能) 及仓库 `COMBAT_SYSTEM.md` 为准。缺证据的适配参数必须保持“暂定”标注。

| 项 | 来源 | 当前实现 | 暂定参数 | 未覆盖例外 |
| --- | --- | --- | --- | --- |
| 索敌排序 | PRTS 作战机制；`COMBAT_SYSTEM.md` §5 | 剩余路程含当前路段。近战：自身阻挡 → 分支特殊（速射手空、神射手防、攻城者重量）→ 嘲讽 → 剩余路程 → uid。高台跳过阻挡键，其余相同。敌方：阻挡者 → 嘲讽 → 最近部署 → uid | 无 | 隐匿被挡后的原作仇恨、3 帧索敌节奏、完整敌人 AI |
| 阻挡 | 同左；`status.js` 控制权限 | `resolveBlocks` 先保留合法旧关系，再按 uid 分配新阻挡。容量读 `blockCnt`，需求读 `blockCost`（缺省 1）。飞行／消失／睡眠／浮空／失去阻挡能力立即解除 | 接触半径 0.72 格 | 多格体型、不可阻挡标签的完整敌人表、工事破坏 |
| 攻击时序 | `COMBAT_SYSTEM.md` §6；攻速公式见 PRTS 属性 | 锁定目标 ID → 前摇帧 → 释放（近战当场命中／远程生成弹体）→ 剩余攻击间隔。多段按模拟时间排队 | 缺单位前摇表时用间隔的 30%；弹速 6 格/秒；多段间隔 2 帧。三者均非原作动画表 | 原始抬手帧、弹速、弹道曲线、攻击重置的客户端量化 |
| 技能衔接 | PRTS 技能分类；`native-sp.js` | 瞬发／持续／弹药。持续与弹药锁技力；通过 `combatFlow.resetAttack` 显式覆盖重置规则；瞬发默认不重置，持续／弹药默认重置。技能结束取消未出手动作并恢复基础攻击 | 默认类型策略仍为适配规则，逐技能原作配置尚未全量取得。原作表 `duration:-1` 只表示“没有固定持续”，必须按三类区分（见下） | 形态切换、召唤、复杂多段技能时间轴 |
| 属性结算 | PRTS 属性公式；`combat.js#attribute` | `V = (基础 + 固定加) × (1 + 同类比例和) × 独立乘积`。盟约／策略／装备／技能写入 `stats().parts` | 已实现盟约／策略／装备比例使用独立倍率，普通技能／分支比例在同类加算层；具体专属效果仍需逐项扩展 | 未实现盟约、元素损伤、目标防御修改以外的独立易伤表 |
| 路线检查点 | 关卡 `MOVE` / `PATROL_MOVE` / `WAIT_FOR_SECONDS` / `DISAPPEAR` / `APPEAR_AT_POS` | `compileRoute` 保留指令；用 `cmd` + `cmdLeft` 推进。消失期间不可见、不可选、不可挡；出现后重算空间关系。不可达抛出“原始路线不可达” | 无 | 随机偏移、巡逻循环、复杂位移技 |
| 位移／换位置落点 | 备战期 `canDeploy` 口径（`native-session.js`）＋ `teleportActor` / `validMoveTile` | 目标格必须可通行，且**没有别的「占格子」单位**——只有干员与占格子的召唤物占格子；**敌人不占格子**（被阻挡时本来就与干员同格），所以换位置不用避开敌人。`canRelocateTo` 再叠加 `buildableType!=='NONE'`、无 `obstacle`、近战不上高台；`nearbySpots` 由近及远给候选格，`projectSpot` 沿朝向由远及近取第一个合法格。乌尔比安 S3 船锚位移期间，他让出的原格对**友方**换位置视为被占据（技能结束要返航），对他自己与敌人的位移不设限 | `nearbySpots` 从敌人身边一路枚举到棋盘边界，取最近空位（只搜 3 圈会在满编阵型里静默不落点）；船锚距离取黑板 `projectile_range` | 传送免疫、失衡免疫与重量判定（见 `ABNORMAL_STATUS_AUDIT.md`） |
| 漏怪掉血上限 | 用户 2026-09-19 口径；`protocol.ROUND_LEAK_CAP` | 每回合（本模式一回合就是一场战斗）漏怪造成的生命扣除**上限 10 点**：结算走 `native-session.finishCurrentBattle` 的 `s.hp -= min(10, result.leaks)`；漏失真值照旧写进 `s.history`、`s.lastBattle.leaks` 与战报，不截断。战斗内的判负条件同样用上限后的值（`min(10, s.leaks) >= hp`，`native-battle.step`），所以上限救得回来的回合不会提前结束 | 上限 10 是项目规定值 | 原作没有这条上限；legacy 演示层（`engine.js` 逐只漏怪立刻扣血）未改 |

本轮按用户“修复直到可通过验收”的要求执行针对性回归和独立浏览器检查，结果见 `COMBAT_ACCEPTANCE.md`。


属性来源补证：[PRTS 下半期盟约记录](https://prts.wiki/w/卫戍协议：盟约_下半/PRTS盟约记录)，2026-09-14 核对属性加成说明。等待／消失指令的 position 为非移动元数据，只有移动和出现指令改变路线位置。

## 阿戈尔与不屈（2026-09-22）

- 阿戈尔先按上到下、左到右的部署顺序建立全部标记，沿被标记阿戈尔的朝向继续追踪，最后统一结算吞噬。A→B→C→D 时，A 获得 B/C/D 的基础攻击力和阻挡数，B 获得 C/D 的，C 获得 D 的；遇非阿戈尔或空格停止。自身、重复目标和已标记自己的来源不能再标记，环路不会无限递归。只读各目标自己的基础属性，攻击力最终加算，阻挡数累加。
- 用户确认吞噬每次固定流失 5000 生命：走 `applyLoss`，不经过防御、法抗、减伤、护盾、脆弱或坚守分摊；不死等致死保护仍走公共入口。流失来源为目标自身，击杀归属为标记来源。初次部署完成后才扣血，避免重新刷满；目标首次击倒（包含复活或切换替身）后取消未触发的标记，同一目标每回合只贡献一次等阶层数。
- 五名不同阿戈尔时，前三名首次击倒各消耗一个复活名额；按 PRTS 口径，单位退场后下一次部署时间和费用归零，吞噬固定流失和连锁属性继承不变。与不屈同次触发仍消费阿戈尔复活名额。
- 不屈覆盖地面干员的击倒、撤退、替身切换，概率为 `0.18 + 0.004 × 层数`（至多 1）；生效时下一次部署冷却和费用均为零，只消费一次标记。三人效果只由地面干员击倒触发，全场回复 5 技力，复活不漏发、同次击倒不重复发。免费部署不产生撤退返费。
- 归溟幽灵鲨与风丸接入本体→替身→本体：替身零阻挡，持续时间取特性黑板；替身期间不开本体技能、不自然回复技力。归溟幽灵鲨的减速与周期法伤只在替身期间生效，S2 结束按击倒结算；切回本体触发对应卫戍与不屈判定。此项不表示所有傀儡师模组专属例外已验收。
- 定向回归：`tests/native-egir-indom.test.mjs`、`tests/native-bonds.test.mjs`、`tests/native-effects.test.mjs`、`tests/native-equipment-combat.test.mjs`。

## 技能持续时间口径（`Battle#skillTimeLeft`）

原作技能表的 `duration:-1` 不是“无限”，它同时覆盖三类语义完全不同的技能，必须分别判定：

| 类别 | 判定依据 | 时长 | 例子 |
| --- | --- | --- | --- |
| 无限持续 | 描述含「可以在下列状态和初始状态间切换」或「持续时间无限」 | 无限（内部 1e9） | 山「横扫架势」；拉普兰德「日晷」；浊心斯卡蒂「同葬无光之愿」；史尔特尔「黄昏」 |
| 有限持续 | 描述带 `{duration}` 占位，真实值在黑板 `duration` | 黑板值 | 雷蛇「充能防御」8 秒；华法琳「不稳定血浆」15 秒；莱恩哈特「解构与爆破」6 秒 |
| 一次性 | 以上皆非（「下次攻击」「抵挡下一次」等） | 不占用持续时间，立即退出激活态 | 小满「竹笛飞声」；风丸「纸艺·迅击」 |

**判定优先级必须是：无限文案 → 正数 `duration` → 黑板 `duration` → 一次性。** 例外：史尔特尔「黄昏」同时带 `{duration}` 占位和黑板 `duration=60`，但描述写明「持续时间无限」，因此归入无限类；若把文案判定放在后面，黄昏会被误算成 60 秒。

`duration` 为正数时始终读该值；`duration:0` 但描述可切换的技能（如山「横扫架势」）同样按无限处理。当前全量分布（按 干员|技能|duration 去重）：无限持续 15 个、黑板取得有限时长 12 个、一次性 40 个，另有 300 个技能直接用正数 `duration`。

2026-09-17 修复：此前把 `duration:-1` 一律写成 1e9，导致一次性技能被永久激活，技力条不回缩、技能也不再释放。回归见 `tests/native-skill-duration.test.mjs`。

## 盟约·突袭的闲置再部署（触发闸门，待后续实机复测）

用户反馈「10 秒未攻击就位移走」体感没生效，当前结论：链路本身可用（真波次战斗里 `t=10.03s` 精确触发一次，`tests/native-bonds.test.mjs` 已固化），但闸门全部成立才会触发，需要实机按这五条复核，暂挂观察：

1. 盟约激活：场上同时 **2 名**带【突袭】的干员（`activeCount=2` / `BOARD`），只有 1 名时效果完全不跑。
2. 触发条件：连续 10 秒「攻击范围内没有敌人」（有敌人进范围即重置计时），**或**技力已满（此时不等 10 秒）。10 秒取自效果黑板 `no_attack_duration`。
3. 场上要有**地面**（`!e.flying`）、非 `hidden` 的敌人当落点目标。
4. 落点：必须存在「能部署该干员且没被别的干员／占格子的召唤物占了」的格子；搜索从敌人身边按圈枚举到整张棋盘，取最近空位（曾经只搜 3 圈，满编阵型下会静默不落点）。
5. 木桩阶段全员在打木桩，属于「范围内有敌人」，不算闲置。

自检：对局内「导出存档」，在 JSON 的 `battle.logicLog` 里搜 `raid-redeploy`——有这条 `move`（带 `fromX/fromY`）即位移确实发生，没有就是上面某条没满足。
