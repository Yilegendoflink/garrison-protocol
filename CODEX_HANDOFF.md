# 卫戍协议网页游戏：Codex 开发交接

地图反馈修正（2026-09-13）：原先将leftNormal整块（含手牌／商店区）显示为战场，且忽略预置工事，混淆可部署性和通行性。现保留原坐标但按实际战场范围显示；整备地块禁止作战部署，旧存档误放单位退回手牌；预置阻隔工事、射击台、浮板接入地块属性；隔离平台与普通道路分别显示。寻路遵循路线运动类型及允许斜向配置，禁止切穿障碍角，无法寻路不再直接穿墙连线。装置动态破坏及特殊环境效果仍待完善。资料：[PRTS 下半战场一览](https://prts.wiki/w/卫戍协议：盟约_下半/战场一览)。本次按用户要求仅构建，未进行功能测试。

2026-09-13 反馈修正：增强高台／地面视觉区分；使用实际波次队列预览本轮及下一轮出怪数；支持场上干员拖回手牌、换位后重新定向。根据PRTS记录71个历史职业分支（本期涉及56个），接入咒愈师伤害转治疗及多种分支基础行为，游戏内可查看规则与未完成项。详见 [BRANCH_RULES.md](BRANCH_RULES.md)。本批只构建发布，未运行功能测试。

更新日期：2026-09-12。将本文件放到解压后的 `garrison-protocol` 根目录，与 `README.md`、`package.json` 放在一起。

本文件是当前对话的项目摘要，不是完整聊天记录；用于让新的 Codex 会话接续现有代码。

## 2026-09-13 GitHub上传与静态Pages部署

仓库已创建并推送：Yilegendoflink/garrison-protocol。用户已明确批准公开整个已说明的范围，不要再次索要同一授权。源码在main，Pages使用gh-pages根目录。自定义Actions的dispatch返回500，已改用静态分支发布；配置源后推送会触发pages build and deployment。备用workflow仅手动触发，当前不要把main推送当作页面同步。详见PUBLISHING.md。

## 2026-09-13 主对局迁移与手动发布

用户最新要求不运行测试，上传GitHub并部署Pages供手动验收。主入口现为 native-play.js/native.bundle.js，旧演示在 legacy.html。新 NativeSession/NativeBattle 已连接原作数据、运营、战斗和阶段；本轮仅构建，未运行测试。

复杂技能／敌人能力／盟约策略／特殊池等仍存在差异，不得宣布完整还原。先读 README.md、MANUAL_RELEASE.md，不能引用上一版75项测试作为本轮验证。工作流不再运行测试和 release:check。

此前公开仓库创建曾被自动审批拒绝。用户随后对具体目标和约310MB公开范围明确回复“就直接上传到我账号仓库并部署就行了”，审批已通过并创建 Yilegendoflink/garrison-protocol；按要求推送并部署，工作流不运行测试。

## 2026-09-12 原作范围与最终木桩

优先读取 [NATIVE_RULES_PROGRESS.md](NATIVE_RULES_PROGRESS.md)。用户要求可用干员、敌人、盟约、策略与 PRTS 下半期完全一致；仅最终 Boss 阶段豁免，改为无限生命木桩，150 秒且可手动结束并播报总伤害。木桩已接入演示战斗和终局，支持独立统计、保存回放、真实浏览器验收。当前 legacy 内容版本 5。

新增 215 条敌人引用和 376 份 PRTS 头像、28 类运营特质解释器及策略准备事件。新 NativeEconomy 仍未接管主对局，不能宣称 S06 已完成。75 项测试通过，release:check 根据 acceptedScopeComplete=false 继续阻止发布。没有创建远程仓库或推送。

## 2026-09-12 发布时点约定

用户明确要求等 S06 全部验收通过后再创建 GitHub 仓库、发布 GitHub Pages。当前没有远程仓库或线上部署，不提前发布试玩版。已准备 Pages 工作流与 release:check，当前未完成报告会阻止发布。完成后的执行顺序见 [PUBLISHING.md](PUBLISHING.md)。

## 2026-09-12 部署修正、资金确认与 S04–S06 推进

当前优先读取 [S4_S6_PROGRESS.md](S4_S6_PROGRESS.md)。两段式部署与真实连续触控已修正；用户确认基础资金为第 n 轮 n+3，无封顶、各难度相同，额外收益另算，勿再次要求确认。规则来源在 data/modes/alliance-lower/confirmed-rules.json。

已取得本期 activity_table 和 50 份关卡模板，增加原作固定养成解析、运营组件、控制状态、次数盾、技能自动操作政策及下半期资料页。legacy 内容版本 4；旧存档备份，暂离期限 24 小时。构建命令已包含 build-protocol。57 项 Node 测试以及桌面、触控、资料页、过期恢复与离线启动验收通过。

S04–S06 未完全通过：原作全技能／敌人能力／天赋模组效果、精确动作时序、原作运营与战斗完整接入、商店权重及双客户端检查仍待完成。资料目录不是技能实现率，readiness.json 的 playableParityComplete 保持 false。当前“开始模拟”仍运行 35 人改编包。

以下按时间倒序保留旧轮次记录；旧测试数量和内容版本不代表当前状态。

## 2026-09-12 S0–S3工程交付

已建立Git基线56a2e4b，归档当前／下半期历史原作表并完成目录映射。新增content.js、session.js、catalog.js、screens.js及prototype.css，主入口现为大厅，可进入调配、数据库、协议、简报和可玩演示阵地。40项单元／集成测试及独立Edge的桌面、触控部署、四种宽度和file URL启动通过。npm run dev在5502端口预览。详见S0_S3_DELIVERY.md；同版本全套UI／PC键位／热修复证据仍未全部取得，不能宣布原作还原完成。

## 2026-09-12 界面与操作还原计划

用户进一步要求界面及操作逻辑接近原作该模式。DEVELOPMENT_PLAN.md已统一为S0–S10，替代此前P/D并列编号；UI_RESTORATION_PLAN.md包含页面状态、操作契约、键鼠／触控和验收方法。数据库仍前置，界面取证与原型从早期推进，S5形成真实单人操作闭环。本轮仅修改计划，尚未实施UI重构；原作完整页面截图、录像和准确键位仍需归档。

## 2026-09-12 原作数据库前置

整体目标与实施顺序见 DEVELOPMENT_PLAN.md。用户指定先建立完整PRTS原作干员／敌人数据库，再完善引擎和卫戍协议下半期覆盖层。当前已完成460个干员、1804个敌人、71个召唤物页面的全目录采集，输出位于 data/prts/snapshots/2026-09-12-prts，含SQLite、JSON、来源和覆盖报告。使用说明与未解决字段见 data/prts/README.md。数据库是参考资料底库，尚未替换游戏内改编数据，也不代表特殊AI已实现。全项目29项测试通过。

## 2026-09-08 本地战斗基础层更新

当前目录已在下述源码包基线上修改。后续优先遵循 [COMBAT_SYSTEM.md](COMBAT_SYSTEM.md)：新增 combat.js、targeting.js、actions.js，已接入属性／伤害／技力、格子范围、索敌、前摇和追踪弹道。新规则版本2，仍兼容旧存档字段迁移。23项自动测试覆盖基础公式与既有玩法，未完成原作数据和特殊机制全量校准。下文“当前基线”提交号是导入来源，不是修改后文件的版本。

## 用户目标与当前优先级

用户原始要求：

1. “制作一版网页可玩的明日方舟 卫戍协议模式自制版本，要求完整实现原有的玩法”。
2. “打包完整源码 以供下载”。
3. 下载后反馈：“启动没有任何画面阿”。
4. 现在希望将当前对话迁移到 Codex 中继续开发。

已经制作了单人网页版本、交付完整源码 ZIP，并修复了找到的启动问题。用户尚未反馈修复版在其电脑上的实际效果。应在现有项目上继续，先确认启动与基本交互，再围绕原始玩法目标补齐差异。

注意：原始目标仍是完整玩法；当前实现未达到原版全量复刻，不应把已实现的单人改编版描述为全部需求完成。

## 导入源码基线（历史记录）

- 源码包：`garrison-protocol-full-source.zip`，解压后顶层目录为 `garrison-protocol/`。
- 修复版源码提交：`3ccb72b31ce81936b6b82c35ea27697794bd2e65`。
- 提交说明：`Fix blank startup and support portable offline HTML launch`。
- 包内 `SOURCE_VERSION.txt` 应包含上述提交号；旧版提交以 `a787ba9` 开头。
- 包内有 `SHA256SUMS.txt`，含完整源码、本地图像、预构建脚本、构建工具、测试与启动说明；不含 `.git` 历史。
- 原在线站点为 https://garrison-protocol.luolinke879.chatgpt.site 。本次白屏修复尚未重新部署到该站点，下载版源码已更新。不要以旧在线站点判断修复版是否生效。

## 启动、开发与测试

玩游戏无需安装依赖：完整解压 ZIP，双击根目录 `index.html`，它会跳转到 `dist/index.html`。也可以直接打开 `dist/index.html`。请从解压后的文件夹运行，不要从压缩包预览窗口运行。

开发使用 Node.js 22 或更新版本。项目无第三方 JavaScript 依赖，无需先执行 `npm install`。

```bash
npm run build
npm test
```

等价命令：

```bash
node scripts/build-browser.mjs
node --test tests/*.test.mjs
```

修改 `dist/` 内原始 JavaScript 模块后必须重新构建 `dist/game.bundle.js`，浏览器实际加载的是这个预构建普通脚本。修改 HTML、CSS 或图片无需构建。Live Server 或其他静态服务器也可以运行；资源路径支持子目录部署。

## 文件定位

| 文件 | 用途 |
| --- | --- |
| `index.html` | 双击启动入口，跳转至 `dist/index.html` |
| `dist/index.html` | 页面结构、相对资源路径、加载与错误提示 |
| `dist/data.js` | 干员、技能、盟约、装备、策略、地图和敌人波次 |
| `dist/engine.js` | 可序列化状态机、运营规则和固定步长战斗模拟 |
| `dist/renderer.js` | Canvas 棋盘、单位、战斗效果与尺寸适配 |
| `dist/app.js` | 中文界面、用户输入、选择流程与自动保存 |
| `dist/style.css` | 深色终端风格与响应布局 |
| `dist/game.bundle.js` | 生成文件，供普通 script 标签离线加载 |
| `scripts/build-browser.mjs` | 不依赖外部包的专用模块打包器 |
| `tests/engine.test.mjs` | 8 项游戏引擎回归测试 |
| `tests/startup.test.mjs` | 3 项启动与路径回归测试 |
| `dist/assets/asset-manifest.json` | 本地图像来源清单 |
| `README.md`、`本地运行说明.md` | 实现范围、运行方式与操作说明 |

打包器针对当前模块使用的具名导入/导出编写；引入新的模块语法时需同步检查打包器的支持范围。不要只改生成文件而遗漏源模块。

## 白屏原因及已做修复

1. `dist/app.js` 的 `renderIntel()` 同一作用域重复声明了 `note`，导致整个脚本解析失败。波次备注变量已改名为 `waveNote`。
2. 原 `/app.js`、`/style.css`、`/assets/...` 根绝对路径在子目录运行时失效，已改为相对路径。
3. 为支持直接双击 HTML，以普通 `defer` 脚本加载 `game.bundle.js`，避免 `file://` 下 ES modules 加载限制。
4. 移除了外部字体依赖，素材保存在项目内；增加可见加载状态和启动错误信息，并处理受限存储、缺少 `ResizeObserver` 的场景。

交付前 11 项测试全部通过。启动测试在 Node VM 中使用有限的浏览器宿主 API 替身，执行实际打包脚本及部分界面操作；这不等于真实浏览器截图检查或端到端验收。尚需在用户实际浏览器环境确认画面、点击和战斗流程。

若仍遇到白屏，优先核对源码版本和是否完整解压，检查浏览器控制台首条错误、页面启动错误提示，以及 `game.bundle.js` 是否由当前源码构建。

## 已实现的玩法

- 35 名干员、6 个调度等阶、15 种盟约、8 种装备、10 种局内策略、3 种初始策略。
- 3 张自制地图、3 种难度、16 轮防卫；特定难度满足无漏怪条件后进入第 17 轮挑战。
- 招募、出售、刷新、冻结、调度升级与回合折扣，战斗开始清空当轮运营预算，结束后重新获得预算。
- 三个同名初始干员合成精锐，保留部署位置并退回装备，同时免费选择高一阶干员。当前没有进一步的三星合成。
- 10 个常规整备位和临时溢出区；相同装备二合一，每名干员最多两件装备，替换装备有明确选择流程。
- 初始 8 个部署位；朝向、射界、地面/高台、移动、交换、阻挡、空中单位与不可阻挡敌人。
- 物理/法术/真实伤害、治疗、范围攻击、多目标、减速、晕眩、削防、护盾、自动技能及费用/冷却约束下的自动再部署。
- 按不同已部署干员激活盟约，并在获得、刷新、回合、技能、击杀或倒地等事件上触发对应效果。
- 阶段策略、领袖战、胜负结算、伤害统计；鼠标/触控/键盘布阵，暂停与 1/2/4 倍速。
- 当前浏览器自动保存完整对局，刷新后恢复同一战斗状态并默认暂停。游戏存档键为 `garrison-protocol-v1`，声音偏好键为 `garrison-sound`。

## 与原始目标仍存在的差异

当前是单人独立改编版。地图、波次、数值以及部分技能、盟约和策略效果经过重制，尚不包含原版全部内容。至少还缺：

- 四人在线同盟模拟及其完整协作规则。
- 全部原版干员与逐项对应的专属技能、盟约、装备和策略。
- 全部历期活动、地图、敌人和完整原版数值对应。
- 官方账号养成及兑换奖励系统。

继续补齐前，应以用户指定的原版期次和公开规则核对具体差异；没有核对的规则标记为待确认，不要用自定义数值充当原版事实。

## 建议接续步骤

1. 阅读本文件顶部、`S4_S6_PROGRESS.md` 和 `README.md`，以当前源码与 Git 状态为准；下方导入包版本号仅供历史排错。
2. 执行构建和既有测试；在允许且具备条件的实际浏览器中确认启动、招募、部署、战斗及刷新恢复。
3. 对照原版目标建立具体差异清单，优先处理阻碍正常游玩的缺陷，再分批补齐玩法，并保留可运行的源码交付。

## 规则与图像来源

现有项目 README 记录的规则参考为 https://prts.wiki/w/卫戍协议 与 https://prts.wiki/w/卫戍协议/帮助 。这些是后续核对的入口，本交接文件未重新验证全部原版规则。

图像来自 https://github.com/ArknightsAssets/ArknightsAssets2 ，具体来源见素材清单。项目为非官方同人作品，原始角色及图像权利属于相应权利人；素材的公开可访问性不代表获得了开放使用许可。
