# 下半期原作规则接入与木桩替代进展

> 2026-09-16：本文停在 09-12。仓库和 Pages 已存在，主入口已是 native 对局，不是 35 人演示。当前事实见 [README.md](README.md) 与 [AGENTS.md](AGENTS.md)。下文只作当时工程记录。

更新：2026-09-12。目标来源为用户指定的 [PRTS 卫戍协议：盟约 下半](https://prts.wiki/w/卫戍协议：盟约_下半)。当时继续开发，尚未达到全范围验收条件；**当时还没有远程仓库或 Pages**（此项已过时）。

## 已确认的范围变更

仅最终 Boss 阶段暂缓原作实现，改为无限生命木桩；沿用原版最终阶段倒计时并允许手动结束，结束后播报总伤害。其余可用干员、敌人、盟约及策略仍要求与下半期一致。非最终阶段通过悬赏等途径出现的 Boss 类型敌人不在此豁免内。

`common.turnInfoDataDict` 中各模式领袖轮的 `bossTurnHpReduceTime` 都是 150 秒。当前木桩测试以此为结束时长；不继续执行领袖超时扣目标生命。木桩不移动、不攻击，防御和法抗 0 为中性测试默认值。规则和用户确认见 `data/modes/alliance-lower/confirmed-rules.json`。

## 本轮可直接使用的内容

- 当前演示阵地提供“当前阵容木桩测试”，木桩位于 F2（第 6 列第 2 行）。演示最终轮也会进入该测试。
- 木桩采用无限生命标记，不以一个巨大 HP 数值替代；全额记录结算伤害，HP 不递减，不产生击杀收益。
- 以 30Hz 模拟帧计时，150 秒自动结束，支持手动结束。报告独立统计本阶段总伤害、DPS、伤害类型和各单位贡献，不混入前面轮次的伤害。
- 结算显示并通过状态提示播报总伤害；开启声音且浏览器支持语音时，会调用中文语音播报。保存／恢复、命令回放和重复结束均有验证。
- 本期资料页增加敌人标签、名称／效果搜索，并使用本地 PRTS 头像。资料完整性不等于主对局已经执行全部原作能力。

## 数据和资源

| 记录 | 当前归档 |
| --- | --- |
| 干员预设 | 133 条源配置、266 条初始／精锐状态；资料页展示 112 条有具体干员的可见预设 |
| 敌人 | 116 条常规随机池记录，加上策略、悬赏和生成模板，共 215 条引用；保留出现来源分类 |
| 敌人属性 | 读取历史基础库，并叠加 `level_autochess_enemy_data.json` 的明确覆盖，未定义字段继承原值 |
| 盟约／策略 | 23 个盟约、40 条策略配置及全部原始参数 |
| 美术资产 | 376 份头像：121 个干员 ID、215 个敌人 ID、40 个策略发起人；含来源 URL、SHA-1／SHA-256，缺失 0 |

素材使用当前 PRTS 提供的默认头像，并非全套历史 Spine 动画归档。资产清单在 `dist/assets/prts/manifest.json`，同步命令为 `npm run data:assets`。

## 新准备阶段控制器

`dist/native-economy.js` 基于真实棋子 ID 处理准备阶段，`dist/garrison.js` 登记了历史表全部 28 类 SERVER 运营特质。`dist/strategy.js` 增加固定回合补给、消费奖励、资金继承、返资和特殊刷新等事件处理。

已验证的规则包括：

- 第三次获得同名初始干员时不额外获得第三名初始单位，而是触发晋升后的精锐获得效果。
- 远见、奇迹、投资人计入整备区；普通盟约按不同干员身份计数。
- 独行只在恰好一个不同身份时有效；绝技计入同名精锐副本；调和增加已出现核心盟约的计数。
- 投资人触发 2 次／100 层后 3 次；远见按阈值发钱且不重复领取，已解锁折扣保留。
- 奇迹概率为 `(18+0.3×层数)%`，仅刷新后剩余免费次数为 0 时判定；助力强化计数区分同名干员的初始／精锐状态。
- 老鲤第 1、2 轮基础预算暂存，第 3 轮为 15；坎诺特继承余额并结算利息。
- 调用 `command` 时，无法解析的奖励池或策略会整体回滚，避免半次购买／扣款。底层方法仅供内部效果或测试场景设置使用。

这些控制器**尚未接管主对局**，随机池分布、部分转阶段奖励／单槽冻结、完整战斗效果仍待接入。策略报告分别列出 serverHooks 和 pendingKeys，不把仅登记的处理器视为完整策略验收通过。

## 验证与剩余工作

当前 75 项 Node 测试通过。真实浏览器验证覆盖本期资料及窄屏、实际头像加载、手动木桩结束、150 秒自动结束及刷新恢复。报告位于 `artifacts/s4-s6/`。木桩场景中，单位贡献之和与总伤害一致；测试不代表全体原作技能完成。

剩余关键工作：原作全干员技能／天赋／模组、非豁免敌人能力、全部战斗盟约和策略效果、真实商店及回合控制器与主界面的整合，以及各难度／组合对照。主入口当前仍运行 35 人 legacy 演示包，内容版本升至 5，旧版本存档保留备份。

发布门槛使用 `s6.acceptedScopeComplete`（用户约定范围），而非把木桩豁免伪称为原作 Boss 完整还原。此值仍为 false；发布工作流继续阻止上传。实际上传与 Pages 发布授权保留，达到条件后执行，无需重复询问。
