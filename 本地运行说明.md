# 卫戍协议 · 启动修复版

## 最简单的启动方式

1. 将 ZIP **全部解压**到一个文件夹，不要在压缩软件内部直接打开 HTML。
2. 双击 `garrison-protocol/index.html`。
3. 页面进入游戏后，点击“开始独立模拟”。

无需安装 Node.js、Python、Live Server 或 npm 依赖。也可以直接双击 `dist/index.html`。

## 使用 VS Code Live Server

打开整个 `garrison-protocol` 文件夹，对根目录的 `index.html` 使用 Open with Live Server 即可。直接访问 `dist/index.html` 也可以。新版使用相对资源路径，不再要求将服务器根目录严格配置为 `dist`。

## 如果仍然无法进入

- 确认 `dist` 中同时存在 `index.html`、`game.bundle.js`、`style.css` 和 `assets` 文件夹。
- 如果覆盖旧包，刷新页面；HTTP 启动可用 Ctrl+F5 强制刷新。
- 页面现在会显示启动错误详情，请将显示的报错发给我即可进一步定位。

## 修改代码与测试

原始游戏逻辑、界面、渲染和数据仍分别保存在 `dist/engine.js`、`dist/app.js`、`dist/renderer.js`、`dist/data.js`，均为可编辑的完整源码。

页面加载的是已构建的 `dist/game.bundle.js`。修改上述原始 JavaScript 文件后，需要在项目根目录执行：

```bash
npm run build
```

运行全部测试：

```bash
node --test tests/*.test.mjs
```

开发工具要求 Node.js 22 或更新版本。只有继续开发或执行测试时需要 Node.js，直接玩游戏不需要。无需 `npm install`。

## 文件与版本

本包包含当前自制单人版本的完整源码、浏览器构建产物、76 张图片素材、已有对局测试和新增启动回归测试。
布阵操作：干员拖到合法地块后进入朝向选择，再拖动朝向并松手确认。取消不会提前放人。
游戏功能和已知范围详见 `README.md`，源码版本见 `SOURCE_VERSION.txt`，文件校验见 `SHA256SUMS.txt`。

角色、图像及其他原始素材的权利归鹰角网络及其关联公司。素材来源保存在 `dist/assets/asset-manifest.json`。
