# S0–S3 交付记录

日期：2026-09-12。目标为国服《卫戍协议：盟约》下半期。

## 阶段状态

| 阶段 | 本次已交付 | 尚未关闭的验收项 |
| --- | --- | --- |
| S0 版本与证据 | Git基线56a2e4b；官方规则来源；PRTS资料修订；当前／历史客户端数据提交；两张可追踪UI参考 | 同版本全页面／完整操作录像和准确PC键位未齐，不能声称S0全部取证完成 |
| S1 数据规范化 | 当前460个干员、2151个敌人内部实体；PRTS全部1804个敌人页面建立对应关系；精确成长、技能、模组、范围；独立历史快照 | 原始数据自身缺少sktok_cdsoul，已保留异常；技能prefab执行逻辑属于S4；服务器热修复仍需进一步核对 |
| S2 工程边界 | JSON内容包校验和注入、命令／预览、原子提交、去重、阶段校验、回放、随机流分离；应用输入已接入 | 当前命令操作者为local；网络传输和多玩家权限按S7实现 |
| S3 页面与操作原型 | 大厅、调配、协议、简报、数据库／详情、房间预览；可玩演示阵地；键鼠与触控；真实浏览器验证 | 原作像素／手势／键位对照尚不能全量通过；当前35名演示池及战斗数值仍属legacy，原型均有标记 |

这些成果是S0–S3的工程交付候选。S0资料缺口和S3原作对照项保持未完成，没有降低或删除整体计划的验收门槛。

## 运行和浏览

- 离线：双击项目index.html。
- 本地服务：`npm run dev`，打开 http://127.0.0.1:5502 。
- 大厅可进入物资调配、原作数据库、单人模拟及同盟房间预览。
- 单人流程：开始模拟 → 难度／演示地图 → 简报／策略 → 建立防线 → 招募 → 点击手牌与地块部署 → 准备完毕。
- 打开详情不会自动冻结战斗；返回大厅会暂停本地演示，可继续／刷新恢复。同盟全局时间尚未实现。
- 原作数据库来自完整规范化资料，显示其与下半期历史快照的存在关系；存在于历史快照不等于在该期可招募。

## 数据分层与S1结果

| 快照 | 客户端／数据 | 干员 | 敌人实体 | 敌人等级 | 技能 | 范围 |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| 当前 | 2.7.71 / 26-09-09 | 460 | 2151 | 2411 | 1810 | 73 |
| 下半期历史 | 2.7.21 / 26-04-13 | 441 | 1859 | 2086 | 1638 | 72 |

PRTS是主要说明和目录来源。Kengxxiao/ArknightsGameData公开镜像作为精确字段补证，原文件、提交号和哈希独立保留；未将镜像标为官方API。

`data/gamedata/`保存固定提交的原表，`data/normalized/`保存规范化产物、双版本SQLite查询库和报告；原有`data/prts/`参考库保持独立，不覆盖它的历史采集记录。

四个特殊映射分别处理：两个虚像条目通过显示名、HP与不攻击行为联合识别；采掘机规范化名称尾换行；重启锚点NPC条目按其列出的名称与属性作为聚合条目映射。不会强制一页只能对应一个内部实体。

历史快照中未出现的后续条目标记not-in-snapshot，不当作漏采。当前全部干员和PRTS敌人页面已有对应记录；数据支持范围仍不等于引擎已支持全部特殊能力。

## S2接口

- `loadContentPack`：从JSON构造索引并验证必需字段、引用和地图路径。
- `new Game(seed, content)`：每个模拟实例显式绑定内容包。
- `GameSession.preview(type,args)`：在隔离副本校验，不修改真实资源或位置。
- `GameSession.dispatch(envelope)`：校验操作者、序号、帧、阶段与修订；同一提交重试不重复执行。
- `InteractionState`：选中、部署预览、提交、取消；过期预览拒绝提交。
- `exportReplay` / `GameSession.replay`：记录输入和初始状态，使用相同内容版本重现结果。
- 玩法随机分为economy与battle，渲染不调用它们。

演示内容的规则哈希由构建脚本计算。旧存档的未知内容版本会拒绝恢复并保留备份，不能静默当成当前内容继续执行。

## S3视觉与交互说明

当前原型从可用参考提取冷青灰、深色功能区、顶部导航、盟约横条和底部调度结构。大厅等未取得完整同版本截图的页面属于明确标识的设计原型，不冒充逐像素复刻。

战场仍使用Canvas和现有素材，投影与区域占比已改善；原作三维场景、Spine动作和全部效果属于后续S4/S9。

保留独立的页面、操作状态和对局状态；界面预览只读。修复了下半期满手牌交换限制，装备替换与取消仍通过统一命令验证。

## 验证产物

- `tests/session.test.mjs`：参数／阶段拒绝、预览取消、过期输入、去重、精确回放、满手牌交换和随机流分离。
- `tests/content.test.mjs`：独立内容包、存档版本、引用错误、全量映射和历史关键参数。
- 原有引擎／数据／启动测试继续保留。
- `artifacts/s0-s3/browser-report.json`：独立Edge浏览器验证报告。
- 同目录截图覆盖大厅、调配、设置、简报、档案、布阵、战斗、320/375/414/768宽度和横屏触控。
- 浏览器验证跑通购买、部署、取消、开战、返回、刷新恢复、触控部署和真正的file URL启动；没有页面运行错误。40项自动测试全部通过。

浏览器工具本身遭遇Windows沙箱ACL启动错误，采用独立、无用户登录资料的Headless Edge + Playwright完成测试。未控制用户现有浏览器会话。

## 后续必须补证

1. 下半期第二阶段PC端各主要页面及完整操作录像，关闭S0和S3的准确键位／视觉对照项。
2. 最终历史规则与服务器热修复的对应关系。
3. 缺失的sktok_cdsoul来源、特殊prefab能力和S4执行映射。
4. 模式名单、盟约、装备、策略及完整单人规则属于S5/S6；当前不把演示规则替换标识去掉。
